---
title: "Think like a Scientist: Physics-guided LLM Agent for Equation Discovery"
title_zh: 像科学家一样思考：用于方程发现的物理引导型 LLM 智能体
authors: "Jianke Yang, Ohm Venkatachalam, Mohammad Kianezhad, Sharvaree Vadgama, Rose Yu"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.12259v2"
tags: ["query:sr:composite"]
score: 10.0
evidence: 物理引导的 LLM 智能体用于方程发现
tldr: 科学研究的核心是通过符号公式解释现象。针对现有大语言模型在方程发现中往往直接从数据猜测而缺乏物理推理的问题，本文提出了KeplerAgent。该框架模拟科学家的思维过程，先利用物理工具推断对称性等结构先验，再指导PySINDy和PySR等符号回归引擎进行搜索。实验证明，该方法在物理方程基准测试中显著提升了符号准确率和抗噪性。
selection_source: fresh_fetch
motivation: 现有的LLM方程发现系统通常直接从数据中猜测公式，缺乏对物理属性（如对称性）的显式建模和推理过程。
method: 提出了KeplerAgent框架，通过协调物理工具提取中间结构，并据此动态配置符号回归引擎的函数库和结构约束。
result: 在多个物理方程基准测试中，KeplerAgent在符号准确率和抗噪鲁棒性方面均显著优于传统符号回归和纯LLM基准。
conclusion: 将物理先验推理与符号回归引擎相结合的智能体框架，能有效提升大语言模型在科学方程发现任务中的表现。
---

## 摘要
通过符号化、可解释的公式来解释观测到的现象是科学的一个基本目标。近年来，大语言模型（LLMs）凭借其广泛的领域知识和强大的推理能力，已成为符号方程发现领域极具前景的工具。然而，大多数现有的基于 LLM 的系统尝试直接从数据中推测方程，而没有建模科学家通常遵循的多步推理过程：即先推断对称性等物理特性，然后将其作为先验知识来限制候选方程的空间。我们引入了 KeplerAgent，这是一个明确遵循该科学推理过程的智能体框架。该智能体协调基于物理的工具来提取中间结构，并利用这些结果来配置 PySINDy 和 PySR 等符号回归引擎，包括它们的函数库和结构约束。在一系列物理方程基准测试中，KeplerAgent 相比于 LLM 和传统基准方法，实现了显著更高的符号准确率以及对噪声数据更强的鲁棒性。

## Abstract
Explaining observed phenomena through symbolic, interpretable formulas is a fundamental goal of science. Recently, large language models (LLMs) have emerged as promising tools for symbolic equation discovery, owing to their broad domain knowledge and strong reasoning capabilities. However, most existing LLM-based systems try to guess equations directly from data, without modeling the multi-step reasoning process that scientists often follow: first inferring physical properties such as symmetries, then using these as priors to restrict the space of candidate equations. We introduce KeplerAgent, an agentic framework that explicitly follows this scientific reasoning process. The agent coordinates physics-based tools to extract intermediate structure and uses these results to configure symbolic regression engines such as PySINDy and PySR, including their function libraries and structural constraints. Across a suite of physical equation benchmarks, KeplerAgent achieves substantially higher symbolic accuracy and greater robustness to noisy data than both LLM and traditional baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Think like a Scientist: Physics-guided LLM Agent for Equation Discovery》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：科学发现的核心是从观测数据中提取可解释的数学公式（符号回归，SR）。虽然大语言模型（LLM）在符号回归中展现了潜力，但现有方法（如 LLM-SR）通常采用“暴力破解”模式，即直接从数据猜测方程，缺乏科学家发现规律时的多步推理过程。
*   **核心问题**：如何让 LLM 像科学家一样工作——先通过实验和工具推断系统的物理特性（如对称性、守恒律、量纲），再利用这些结构化先验来约束和引导符号回归引擎，从而提高复杂方程发现的准确性和效率。

### 2. 方法论：KeplerAgent 框架
*   **核心思想**：构建一个基于 ReAct 模式的智能体，它不直接输出方程，而是协调一组物理分析工具来提取中间结构信息，并动态配置符号回归后端（PySINDy 和 PySR）。
*   **关键技术组件**：
    *   **智能体架构**：包含系统提示词、工具规格说明、工作区（存储中间文件）和经验日志（记录历史尝试）。
    *   **物理工具集**：
        *   **Python 代码解释器**：用于基础统计分析和探索性数据分析（EDA）。
        *   **视觉子智能体**：分析数据轨迹图，识别周期性、趋势、渐近行为等视觉特征。
        *   **对称性发现工具**：利用神经网络代理模型和无穷小生成器（Lie Symmetry）算法，自动识别系统中的连续对称性（如旋转、缩放）。
    *   **符号回归后端配置**：
        *   **PySINDy**：智能体根据发现的对称性配置“等变约束”，在稀疏回归中剔除不符合物理规律的项。
        *   **PySR**：智能体根据推理结果提供“方程模板”（如 $f(x) + g(y)$），大幅缩小遗传算法的搜索空间。
*   **算法流程**：分析数据 -> 调用物理工具提取结构 -> 解释工具输出（如将数值对称矩阵转化为符号形式） -> 配置并运行 SR 工具 -> 根据误差反馈迭代优化。

### 3. 实验设计
*   **数据集**：
    *   **LSR-Transform**：包含 111 个来自 AI Feynman 基准的物理方程，经过变量转换以防止 LLM 记忆原始公式。
    *   **DiffEq 基准**：作者自定义的 10 个复杂动力系统（含耦合 ODE 和 PDE），如捕食者-猎物模型、范德波尔振子、反应扩散方程等。
*   **对比方法**：
    *   **PySR**：传统的基于遗传编程的符号回归工具（固定配置）。
    *   **LLM-SR**：目前最先进的基于 LLM 的符号回归框架（直接生成代码骨架）。
*   **评估指标**：符号准确率（SA）、归一化均方误差（NMSE）、长时预测误差（针对微分方程）。

### 4. 资源与算力
*   **模型后端**：实验主要使用 **GPT-4o-mini** 作为智能体和视觉子智能体的骨干模型。
*   **算力说明**：论文未详细列出具体的 GPU 型号和训练时长，但强调了**效率优势**。KeplerAgent 的运行时间（约 200-700 秒）和 Token 消耗远低于 LLM-SR，因为它通过物理先验减少了无效的采样尝试。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 111 个代数方程和 20 个微分方程（10 个系统，每个系统 2 个方程），并分别在**干净数据**和**噪声数据**（0.01 噪声水平）下进行了测试。
*   **充分性评价**：实验设计较为充分。除了标准的数值拟合误差，还引入了物理系统特有的“长时模拟误差”来验证发现方程的动力学一致性。通过对比单次运行（@1）和多次运行（@3）的结果，展示了方法的稳健性。

### 6. 主要结论与发现
*   **性能领先**：在 LSR-Transform 上，KeplerAgent 的符号准确率（42.34%）优于 LLM-SR（31.53%）和原生 PySR。
*   **微分方程优势显著**：在复杂的耦合 ODE/PDE 系统中，KeplerAgent 的符号准确率（75%）远超基准（30%-40%），尤其在噪声环境下表现出极强的鲁棒性。
*   **推理价值**：实验证明，LLM 能够理解物理工具输出的数值结果（如对称矩阵），并将其转化为精确的数学约束，这是提升 SR 性能的关键。

### 7. 优点
*   **模拟科学思维**：将“结构发现”与“参数拟合”解耦，符合科学研究的真实逻辑。
*   **模块化与可扩展性**：工具箱可以轻松集成更多物理工具（如量纲分析、分叉分析）。
*   **自动化配置**：解决了传统 SR 工具（如 PySR）极度依赖专家手动调参的痛点。
*   **高效率**：通过精准的结构约束，避免了在大空间内的盲目搜索，显著降低了计算成本。

### 8. 不足与局限
*   **上下文压力**：随着工具集的增加，工具说明和历史日志会迅速填满 LLM 的上下文窗口，可能导致推理能力下降。
*   **迭代陷入僵局**：在某些噪声极大的任务中，智能体可能会陷入重复调用同一工具的死循环，缺乏更高级的策略来跳出局部最优。
*   **对称性工具限制**：目前的对称性发现工具主要针对因变量的线性对称，对于更复杂的非线性对称或时空对称支持有限。
*   **依赖底层模型**：系统的上限仍受限于 GPT-4o-mini 等基础模型的逻辑推理能力。

（完）
