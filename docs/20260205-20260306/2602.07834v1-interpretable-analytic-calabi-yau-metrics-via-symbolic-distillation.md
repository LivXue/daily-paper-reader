---
title: Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation
authors: D Yang Eng
date: 2026-02-08
pdf: "https://arxiv.org/pdf/2602.07834v1"
tags: ["query:sr"]
score: 10.0
evidence: 利用符号回归将神经网络近似蒸馏为物理学中可解释的公式
tldr: 本研究针对弦理论中难以计算的Calabi-Yau流形度规，提出了一种基于符号回归的蒸馏方法。通过将复杂的神经网络近似模型转化为简洁的解析公式，研究者仅用五个项就达到了0.9994的拟合精度，参数量较原模型减少了3000倍。该公式不仅在不同模参数范围内保持稳定，还能准确复现体积积分和汤川耦合等关键物理观测值，成功将黑盒模型转化为可解释的数学表达。
selection_source: fresh_fetch
motivation: 针对弦理论中Calabi-Yau流形度规难以解析计算且神经网络模型缺乏可解释性的挑战。
method: 利用符号回归技术将预训练的神经网络度规近似模型蒸馏为简洁、可解释的数学公式。
result: 得到的五项表达式以极少的参数量实现了0.9994的高精度，并成功复现了体积积分和汤川耦合等物理观测值。
conclusion: 符号蒸馏证明了可以从黑盒神经网络中恢复出紧凑且具有物理意义的解析模型，为复杂几何量的研究提供了新途径。
---

## Abstract
Calabi--Yau manifolds are essential for string theory but require computing intractable metrics. Here we show that symbolic regression can distill neural approximations into simple, interpretable formulas. Our five-term expression matches neural accuracy ($R^2 = 0.9994$) with 3,000-fold fewer parameters. Multi-seed validation confirms that geometric constraints select essential features, specifically power sums and symmetric polynomials, while permitting structural diversity. The functional form can be maintained across the studied moduli range ($ψ\in [0, 0.8]$) with coefficients varying smoothly; we interpret these trends as empirical hypotheses within the accuracy regime of the locally-trained teachers ($σ\approx 8-9\%$ at $ψ\neq 0$). The formula reproduces physical observables -- volume integrals and Yukawa couplings -- validating that symbolic distillation recovers compact, interpretable models for quantities previously accessible only to black-box networks.

---

## 论文详细总结（自动生成）

这篇论文题为《Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation》，由 D Yang Eng 撰写。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在弦理论中，Calabi-Yau (CY) 三倍体的 Ricci 平坦度规对于计算粒子物理观测值（如汤川耦合）至关重要。然而，求解定义度规的 Monge-Ampère 方程在计算上极其困难。
*   **核心问题**：虽然近年来的神经网络（NN）代理模型可以快速近似度规，但它们是“黑盒”，缺乏数学可解释性。论文探讨是否能通过符号回归将复杂的神经网络模型“蒸馏”为简洁、可解释的解析公式，并研究这些公式在模空间（Moduli Space）中是否具有结构不变性。

### 2. 论文提出的方法论
*   **核心思想**：采用“教师-学生”蒸馏框架。首先训练一个高精度的代数度规（教师模型），然后利用符号回归（学生模型）从中提取简洁的数学表达式。
*   **关键技术细节**：
    *   **目标函数**：回归目标是度规行列式与 Fubini-Study 度规行列式比值的对数：$y = \log(\det g_{alg} / \det g_{FS})$。
    *   **特征工程**：利用规范不变的几何不变量，包括幂和（Power Sum）$p_2$ 和第三初等对称多项式 $\sigma_3$。
    *   **符号回归**：使用 PySR 工具，通过帕累托准则（Pareto Criterion）平衡公式复杂度和拟合精度。
    *   **因式分解假设**：提出模空间因式分解猜想，即度规修正可以表示为“固定的函数结构”与“随模参数 $\psi$ 平滑变化的系数”之积。

### 3. 实验设计
*   **数据集/场景**：研究了 Dwork 家族的五次超曲面（Quintic Hypersurfaces），模参数范围 $\psi \in [0, 0.8]$。
*   **Benchmark（基准）**：
    *   **教师模型**：Donaldson 算法生成的 $k=10$ 阶 H-矩阵（875个参数）。
    *   **对比方法**：Ashmore 等人的神经网络模型（约15,000个参数）、3阶多项式拟合。
*   **物理验证**：通过计算体积积分（Volume Integrals）和汤川耦合（Yukawa Couplings）来验证公式的物理有效性。

### 4. 资源与算力
*   **硬件**：使用了 NVIDIA A100 GPU（Google Colab Pro 环境）。
*   **训练细节**：符号回归运行了 200 次迭代，种群大小为 60。神经网络教师模型在每个 $\psi$ 点训练 15 个 epoch。
*   **效率提升**：符号公式的推理速度比神经网络快 3,000 倍，比传统的 Donaldson 矩阵计算快 10,000 倍。

### 5. 实验数量与充分性
*   **实验规模**：在每个 $\psi$ 点采样了 $10^5$ 个点进行训练。
*   **充分性验证**：
    *   **多种子验证**：进行了 10 组独立运行的种子实验，确认了特征（$p_2, \sigma_3$）的稳健性。
    *   **消融实验**：通过移除 $\sigma_3$ 验证了其在捕捉各向异性方面的必要性（误差降低了 50%）。
    *   **跨阶验证**：在 $k=6, 8, 10$ 不同近似阶数下验证了公式结构的稳定性。
    *   **统计检验**：进行了置换检验（Permutation Test）和残差分析，确保结果的显著性。

### 6. 论文的主要结论与发现
*   **极度压缩**：发现了一个仅含 **5 个项** 的解析公式，在 Fermat 点达到了 $R^2 = 0.9994$ 的精度，参数量减少了 3,000 倍。
*   **分层调制原理**：在模空间中，公式的函数形式保持不变，但系数表现出分层特性：奇异项（Singular terms）发生符号反转，而对称项随 $\psi$ 单调增强。
*   **物理一致性**：该公式能以约 2% 的相对误差追踪体积积分，并精确复现了汤川耦合的拓扑值（$\kappa_{111}=5$）。

### 7. 优点
*   **可解释性强**：将黑盒模型转化为具有明确几何意义（如径向各向异性、角度结构）的数学项。
*   **发现新结构**：符号回归自动发现了多项式拟合无法捕捉的奇异项（如 $1/p_2^2$），这与轨道流形（Orbifold）的渐近几何预期一致。
*   **泛化潜力**：提出的因式分解原理为研究其他复杂的物理 PDE 提供了通用范式。

### 8. 不足与局限
*   **教师模型限制**：在 $\psi \neq 0$ 的变形区域，教师模型的 Ricci 平坦误差较高（8-9%），这限制了符号公式在这些区域的绝对几何精度。
*   **覆盖范围**：实验仅限于 Dwork 五次家族，尚未在更高维的模空间或其他 CY 家族（如双三次曲面）上广泛验证。
*   **全局误差**：体积积分的误差（5.5%）高于局部点误差，反映了蒙特卡洛积分方差的累积。
*   **依赖先验特征**：虽然公式是自动发现的，但输入的特征（$p_2, \sigma_3$）仍依赖于对对称性的预先理解。

（完）
