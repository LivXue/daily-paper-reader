---
title: "Learning Gradient Flow: Using Equation Discovery to Accelerate Engineering Optimization"
title_zh: 学习梯度流：利用方程发现加速工程优化
authors: "Grant Norman, Conor Rowan, Kurt Maute, Alireza Doostan"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13513v2"
tags: ["query:sr"]
score: 9.0
evidence: 动力系统的驱动方程发现
tldr: 本研究针对工程优化中目标函数及其梯度评估成本高昂的问题，提出了一种基于数据驱动方程发现的“学习梯度流”（LGF）优化器。该方法通过分析梯度下降、牛顿法或ADAM等算法的优化轨迹，学习其连续时间动力学特征，并构建多项式阶数的代理模型。LGF能在全维或降维空间中运行，有效替代原始优化过程。实验证明，该方法在拓扑优化和逆问题等多个工程领域显著加速了收敛，减少了计算开销。
selection_source: fresh_fetch
motivation: 旨在解决传统工程优化中因频繁评估复杂目标函数及其梯度而导致的巨大计算负担。
method: 引入LGF优化器，利用方程发现技术从优化变量的轨迹数据中学习梯度流动力学，并将其作为代理模型进行求解。
result: 在结构拓扑优化、逆问题及科学机器学习等标准工程问题上，该方法成功捕捉了优化轨迹的关键特征并大幅提升了收敛效率。
conclusion: 学习梯度流为加速高成本工程优化提供了一种有效途径，通过模拟优化动力学显著降低了对原始目标函数的依赖。
---

## 摘要
在这项工作中，我们研究了利用动力系统的数据驱动方程发现技术，来建模和预测无约束优化问题的连续时间动力学。为了避免对目标函数及其梯度进行昂贵的评估，我们利用优化变量的轨迹数据来学习与梯度下降、牛顿法和 ADAM 优化相关的连续时间动力学。随后，将发现的梯度流作为原始优化问题的代理模型进行求解。为此，我们引入了学习梯度流 (LGF) 优化器，该优化器能够在优化过程中以用户定义的时间间隔，在全维或降维空间中构建变多项式阶数的代理模型。我们在工程力学和科学机器学习的几个标准问题上证明了该方法的有效性，包括两个反问题、结构拓扑优化以及两个具有不同离散化的正向求解问题。我们的结果表明，学习到的梯度流通过捕捉优化轨迹的关键特征，同时避免对目标及其梯度进行昂贵的评估，可以显著加速收敛。

## Abstract
In this work, we investigate the use of data-driven equation discovery for dynamical systems to model and forecast continuous-time dynamics of unconstrained optimization problems. To avoid expensive evaluations of the objective function and its gradient, we leverage trajectory data on the optimization variables to learn the continuous-time dynamics associated with gradient descent, Newton's method, and ADAM optimization. The discovered gradient flows are then solved as a surrogate for the original optimization problem. To this end, we introduce the Learned Gradient Flow (LGF) optimizer, which is equipped to build surrogate models of variable polynomial order in full- or reduced-dimensional spaces at user-defined intervals in the optimization process. We demonstrate the efficacy of this approach on several standard problems from engineering mechanics and scientific machine learning, including two inverse problems, structural topology optimization, and two forward solves with different discretizations. Our results suggest that the learned gradient flows can significantly expedite convergence by capturing critical features of the optimization trajectory while avoiding expensive evaluations of the objective and its gradient.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Gradient Flow: Using Equation Discovery to Accelerate Engineering Optimization》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：在工程设计（如拓扑优化、逆问题）和科学机器学习中，优化过程通常需要成百上千次迭代。每次迭代都需要评估目标函数及其梯度，这往往涉及昂贵的偏微分方程（PDE）数值求解，导致计算成本极高。
*   **核心问题**：能否利用已有的优化轨迹数据，通过“学习”优化的动力学规律，构建一个廉价的代理模型来预测后续的优化步骤，从而跳过昂贵的真实梯度计算？
*   **整体含义**：论文将优化算法（如梯度下降）视为连续时间动力系统（梯度流），并首次提出利用数据驱动的方程发现技术（如 SINDy）在线学习这些动力学方程，以加速工程优化。

### 2. 方法论：学习梯度流 (LGF)
*   **核心思想**：将优化变量 $a$ 的演化视为 ODE 系统 $\frac{da}{dt} = f(a)$。在优化的初始阶段或特定间隔，收集一小段轨迹数据，利用方程发现技术拟合出 $f(a)$ 的近似函数 $\hat{f}(a)$，然后通过求解这个廉价的 ODE 来代替真实的优化迭代。
*   **关键技术细节**：
    *   **SINDy 框架**：使用稀疏识别非线性动力学（SINDy）方法，将 $\hat{f}$ 表示为多项式基函数的线性组合，通过回归分析确定系数。
    *   **多优化器支持**：
        *   **梯度下降 (GD)**：直接学习梯度的负值。
        *   **牛顿法**：学习包含黑塞矩阵逆的演化方向。
        *   **ADAM**：通过引入动量和二阶矩的辅助变量，构建增广的 ODE 系统，并学习其中的梯度项。
    *   **降维技术 (SVD)**：针对高维问题（如上万个设计变量），先通过奇异值分解（SVD）将变量投影到低维潜空间（Latent Space），在低维空间学习动力学后再映射回原空间。
    *   **定期重训练 (Scheduled Retraining)**：采用“采样 $K$ 步 -> 预测 $M-K$ 步”的循环模式，以应对代理模型随时间推移产生的漂移。

### 3. 实验设计
*   **实验场景**：
    1.  **1D 动态逆问题**：识别受热棒的材料传导系数（2个变量）。
    2.  **3D 结构拓扑优化**：最小化合规性（13,824个变量，高维挑战）。
    3.  **非线性热传导**：基于 P-Laplace 方程的能量最小化（牛顿法测试）。
    4.  **全波反演 (FWI)**：地球物理中的非凸逆问题（ADAM 测试）。
    5.  **Deep Ritz 方法**：使用神经网络求解弹性力学问题（随机梯度/噪声测试）。
*   **Benchmark**：标准的梯度下降、牛顿法和 ADAM 优化器。
*   **对比指标**：收敛轨迹的一致性、最终物理场误差、梯度评估次数的节省比例。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU 种类、数量）或具体的训练总时长。
*   **软件实现**：提到使用了 PyTorch 框架进行自动微分，并使用 `torchdiffeq` 库中的 `dopri5`（自适应步长 ODE 求解器）进行时间积分。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 5 组具有代表性的工程力学和机器学习实验。
*   **充分性评价**：
    *   **多样性**：涵盖了从低维到高维、从线性到非线性、从确定性梯度到随机梯度的多种情况，实验设计较为全面。
    *   **客观性**：通过设置不同的加速比（如 33% 到 250%）展示了方法的性能边界，并诚实地记录了当加速过快（预测步长过长）时模型失效的情况。
    *   **公平性**：LGF 与基准方法使用相同的学习率和初始化条件，对比具有说服力。

### 6. 主要结论与发现
*   **显著加速**：LGF 在保持物理精度的前提下，能够减少 33% 至 250% 的昂贵梯度评估次数。
*   **内在低维性**：实验发现，即使是上万维的拓扑优化问题，其优化轨迹往往可以被极低维（如 $r=2$）的潜空间动力学准确捕捉。
*   **动力学一致性**：学习到的代理模型在短中期内能极好地模拟牛顿法和 ADAM 的复杂行为。

### 7. 优点与亮点
*   **非侵入性**：该方法不需要修改原始物理求解器，只需观测优化变量的轨迹即可。
*   **在线学习**：无需离线预训练大量数据，在优化过程中实时构建代理模型，适应性强。
*   **理论联系实际**：将深奥的连续时间优化理论（梯度流）转化为实用的工程加速工具。

### 8. 不足与局限
*   **超参数依赖**：历史窗口大小 $K$ 和预测步长 $M$ 的选择目前依赖经验，缺乏自动化的启发式标准。
*   **约束处理**：目前主要针对无约束优化。对于带有硬约束（如投影操作）的优化问题，轨迹可能存在不连续性，LGF 的平滑多项式假设可能失效。
*   **误差累积风险**：作为一种开环预测方法，如果代理模型在关键分叉点预测错误，可能导致优化陷入错误的局部极小值或发散。

（完）
