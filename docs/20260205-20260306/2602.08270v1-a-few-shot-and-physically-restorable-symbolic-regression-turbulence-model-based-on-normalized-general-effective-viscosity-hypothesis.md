---
title: A few-shot and physically restorable symbolic regression turbulence model based on normalized general effective-viscosity hypothesis
title_zh: 基于归一化广义有效粘性假设的小样本且物理可还原的符号回归湍流模型
authors: "Ziqi Ji, Penghao Duan, Gang Du"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08270v1"
tags: ["query:sr"]
score: 10.0
evidence: 物理可恢复的符号回归湍流模型
tldr: 本研究针对RANS湍流模型精度低及数据驱动模型泛化性差的挑战，提出一种基于归一化广义有效粘度假设的符号回归湍流模型。该模型具有“少样本”和“物理可还原”两大特性，仅需少量周期山流动数据训练，即可在翼型和跨声速压缩机等多种复杂流动中展现出优于基准模型的性能。此外，模型在特定物理条件下能自动回归至基准形式，兼顾了数据驱动的灵活性与传统物理模型的稳定性。
selection_source: fresh_fetch
motivation: 旨在解决传统RANS模型精度不足以及现有数据驱动湍流模型在泛化性和物理一致性方面的局限性。
method: 基于归一化广义有效粘度假设，利用少量DNS数据通过符号回归构建具有物理可还原特性的湍流模型。
result: 该模型在周期山、NACA0012翼型及NASA Rotor 37跨声速压缩机等多种异构流动场景中均显著优于基准模型。
conclusion: 研究证明了符号回归方法在构建少样本、高泛化性且能保持物理特性的湍流模型方面的有效性。
---

## 摘要
湍流是自然过程和工程应用中普遍存在的一种复杂、不规则的流动现象。雷诺平均纳维-斯托克斯（RANS）方法由于其计算成本低，已成为快速模拟工程湍流问题的主要方法。然而，经典湍流模型的不准确性构成了 RANS 框架的主要缺点。随着数据驱动方法的快速发展，许多数据驱动的湍流模型被提出，但它们仍面临泛化性和准确性的问题。在这项工作中，我们提出了一种基于归一化广义有效粘性假设的小样本、物理可还原的符号回归湍流模型。小样本表示我们的模型仅在涵盖窄范围湍流物理特性的有限流动构型上进行训练，但仍能在显著不同的湍流中优于基准模型。物理可还原意味着仅使用符号回归训练结果，我们的模型在满足特定物理条件的区域内可以近乎还原为基准模型。归一化广义有效粘性假设是在我们之前的研究中提出的。具体而言，我们首先形式化了小样本数据驱动湍流模型的概念。其次，我们仅使用三维周期山流动切片的直接数值模拟（DNS）数据来训练我们的符号回归湍流模型。第三，我们在周期山流动、零压力梯度平板流动、NACA0012 翼型流动以及 NASA Rotor 37 跨声速轴流压气机流动上评估了我们的模型。我们的其中一个符号回归湍流模型始终优于基准模型，并且我们进一步证明了该模型在某些流动区域可以近乎还原为基准行为。

## Abstract
Turbulence is a complex, irregular flow phenomenon ubiquitous in natural processes and engineering applications. The Reynolds-averaged Navier-Stokes (RANS) method, owing to its low computational cost, has become the primary approach for rapid simulation of engineering turbulence problems. However, the inaccuracy of classical turbulence models constitutes the main drawback of the RANS framework. With the rapid development of data-driven approaches, many data-driven turbulence models have been proposed, yet they still suffer from issues of generalizability and accuracy. In this work, we propose a few-shot, physically restorable, symbolic regression turbulence model based on the normalized general effective-viscosity hypothesis. Few-shot indicates that our model is trained on limited flow configurations spanning only a narrow subset of turbulent flow physics, yet can still outperform the baseline model in substantially different turbulent flows. Physically restorable means our model can nearly revert to the baseline model in regimes satisfying specific physical conditions, using only the symbolic regression training results. The normalized general effective-viscosity hypothesis was proposed in our previous study. Specifically, we first formalize the concept of few-shot data-driven turbulence models. Second, we train our symbolic regression turbulence models using only direct numerical simulation (DNS) data for three-dimensional periodic hill flow slices. Third, we evaluate our models on periodic hill flows, zero pressure gradient flat plate flow, NACA0012 airfoil flows, and NASA Rotor 37 transonic axial compressor flows. One of our symbolic regression turbulence models consistently outperforms the baseline model, and we further demonstrate that this model can nearly revert to baseline behavior in certain flow regimes.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合物理启发与数据驱动技术的符号回归湍流模型。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的雷诺平均纳维-斯托克斯（RANS）模型在处理复杂分离流时精度不足，而现有的数据驱动湍流模型往往面临**泛化性差**（难以应用于训练集之外的工况）和**物理一致性缺失**（在简单流动中无法还原经典模型）的问题。
*   **研究动机**：
    *   获取高保真湍流数据（DNS/LES）成本极高。
    *   旨在开发一种“小样本”（Few-shot）模型，即仅利用极少数流动工况进行训练，就能在完全不同的工程流动中表现优异。
    *   追求“物理可还原性”，使模型在经典理论适用的区域能自动回归到基准模型。

### 2. 方法论：核心思想与关键技术
*   **归一化广义有效粘性假设（NGEVH）**：
    *   基于 Pope (1975) 的理论，将雷诺应力各向异性张量 $\mathbf{b}$ 表示为张量基底 $\mathbf{T}_i$ 的线性组合。
    *   **创新点**：对张量基底进行归一化处理，使得系数 $\hat{g}_i$ 的绝对值能直接反映各项的贡献程度，提高了模型的数值稳定性和泛化能力。
*   **符号回归（Symbolic Regression, SR）**：
    *   利用 **PySR** 库，通过进化算法自动从数据中搜索显式的数学表达式。
    *   **输入特征**：包括 7 个张量不变量（如应变率、旋转率、压力梯度、动能梯度的迹）和 5 个额外物理特征（如壁面距离雷诺数、湍流时间尺度比等）。
    *   **模型构建**：对比了 **SR 3T**（包含 2D 流动相关的 3 个张量）和 **SR 5T**（包含 5 个张量）两种模型。
*   **物理还原机制**：
    *   固定 $\hat{g}_1$ 项以保持与基准 Boussinesq 假设的一致性。
    *   通过符号回归学习其他系数，使其在特定物理极限下（如近壁区或各向同性区）趋于零，从而实现物理还原。

### 3. 实验设计
*   **训练集**：仅使用三维周期山（Periodic Hill）流动的 DNS 数据切片（$\alpha=0.8, 1.2$）。
*   **测试场景（跨度极大）**：
    1.  **周期山流动**（内插与外推：$\alpha=0.5, 1.0, 1.5$）。
    2.  **零压力梯度平板流动**（验证壁面边界层预测能力）。
    3.  **NACA0012 翼型流动**（高雷诺数、分离流）。
    4.  **NASA Rotor 37 跨声速压气机**（复杂 3D、可压缩、强激波工程应用）。
*   **基准模型（Benchmark）**：经典 $k-\omega$-SST 模型，以及针对 Rotor 37 的专门神经网络模型（SA-NN）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU/CPU 数量）或总训练时长。
*   **算法参数**：详细列出了 PySR 的超参数，包括 16 个种群、每个种群 100 个个体、2000 次迭代、最大方程复杂度为 20。作者提到由于计算成本限制，未进行大规模超参数搜索。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单 2D 不可压缩流到复杂 3D 跨声速可压缩流的 4 类典型工况。
*   **充分性评价**：实验设计非常**充分且具有挑战性**。模型仅在简单的周期山数据上训练，却在完全异构的压气机转子工况下进行了验证。这种“跨领域”的测试极大地增强了结论的说服力。
*   **客观性**：通过 MSE 误差定量分析和流场云图定性对比，客观展示了 SR 5T 模型在多数情况下优于基准模型。

### 6. 主要结论与发现
*   **SR 5T 模型表现最优**：在所有测试案例中，SR 5T 均优于基准 SST 模型，而 SR 3T 提升有限甚至在某些情况下变差。
*   **小样本泛化成功**：证明了仅靠周期山数据训练的符号模型可以外推至跨声速压气机等复杂工程场景。
*   **物理还原性验证**：在平板流动的粘性底层和压气机下游的各向同性区域，SR 5T 模型能自动关闭非线性项，回归至经典 Boussinesq 形式。
*   **关键项发现**：分析表明 $\hat{g}_6 \hat{\mathbf{T}}_6$ 项是改善分离流预测的关键。

### 7. 优点（亮点）
*   **可解释性**：符号回归生成的显式代数方程比黑盒神经网络更易于物理分析和工程实现。
*   **极高的数据效率**：实现了真正的“小样本学习”，降低了对昂贵 DNS 数据的依赖。
*   **鲁棒性**：模型直接嵌入 RANS 求解器（TCAE/OpenFOAM），在跨声速等严苛条件下保持了良好的数值稳定性。

### 8. 不足与局限
*   **方程选择的主观性**：在符号回归生成的多个候选方程中，最终选择仍依赖于专家的经验判断（平衡复杂度与精度），缺乏完全自动化的严谨标准。
*   **局部过拟合风险**：在 NASA Rotor 37 的上游区域，模型出现了非预期的激活，作者认为这可能是由于周期山训练数据的局限性导致的局部过拟合。
*   **计算开销**：虽然推理快，但符号回归的训练过程（进化计算）通常比梯度下降法更耗时，且难以在大规模数据集上扩展。

（完）
