---
title: "Prior-Guided Symbolic Regression: Towards Scientific Consistency in Equation Discovery"
title_zh: 先验引导的符号回归：迈向方程发现中的科学一致性
authors: "Jing Xiao, Xinhai Chen, Jiaming Peng, Qinglin Wang, Menghan Jia, Zhiquan Lai, Guangping Yu, Dongsheng Li, Tiejun Li, Jie Liu"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13021v2"
tags: ["query:sr"]
score: 10.0
evidence: 旨在实现科学一致性的先验引导符号回归
tldr: 符号回归常面临“伪方程陷阱”，即方程虽拟合数据但违背科学原理。本文提出PG-SR框架，通过预热、演化和精炼三阶段流程，引入先验约束检查器和先验退火约束评估（PACE）机制，引导搜索过程符合领域先验。理论证明该方法降低了假设空间复杂度，实验显示其在多领域优于现有方法，且对噪声和数据稀缺具有强鲁棒性。
selection_source: fresh_fetch
motivation: 现有的符号回归方法过度依赖经验风险最小化，导致生成的方程往往符合观测数据但违背基本的科学常识。
method: 提出PG-SR框架，利用先验约束检查器将领域知识转化为可执行程序，并通过先验退火约束评估机制在演化过程中逐步强化科学一致性。
result: 实验结果表明，PG-SR在多个学科领域的方程发现任务中均优于最先进的基准模型，并在噪声和少样本场景下表现出极高的稳健性。
conclusion: 通过显式引入科学先验约束，PG-SR不仅解决了伪方程问题，还为发现具有科学一致性和强泛化能力的方程提供了理论保障和实践路径。
---

## 摘要
符号回归 (SR) 旨在从观测数据中发现可解释的方程，具有揭示自然现象背后潜在原理的潜力。然而，现有方法往往会陷入“伪方程陷阱”：生成的方程虽然能很好地拟合观测数据，但与基本科学原理不一致。一个关键原因是这些方法由经验风险最小化主导，缺乏确保科学一致性的显式约束。为了弥补这一差距，我们提出了 PG-SR，这是一个基于预热、进化和细化三阶段流水线构建的先验引导符号回归框架。在整个流水线中，PG-SR 引入了一个先验约束检查器，将领域先验显式编码为可执行的约束程序，并在进化阶段采用先验退火约束评估 (PACE) 机制，逐步引导发现过程转向科学一致的区域。在理论上，我们证明了 PG-SR 降低了假设空间的 Rademacher 复杂度，从而产生了更紧致的泛化界限，并为防止伪方程提供了保证。在实验上，PG-SR 在多个领域均优于最先进的基准方法，对不同的先验质量、噪声数据和数据稀缺性保持了鲁棒性。

## Abstract
Symbolic Regression (SR) aims to discover interpretable equations from observational data, with the potential to reveal underlying principles behind natural phenomena. However, existing approaches often fall into the Pseudo-Equation Trap: producing equations that fit observations well but remain inconsistent with fundamental scientific principles. A key reason is that these approaches are dominated by empirical risk minimization, lacking explicit constraints to ensure scientific consistency. To bridge this gap, we propose PG-SR, a prior-guided SR framework built upon a three-stage pipeline consisting of warm-up, evolution, and refinement. Throughout the pipeline, PG-SR introduces a prior constraint checker that explicitly encodes domain priors as executable constraint programs, and employs a Prior Annealing Constrained Evaluation (PACE) mechanism during the evolution stage to progressively steer discovery toward scientifically consistent regions. Theoretically, we prove that PG-SR reduces the Rademacher complexity of the hypothesis space, yielding tighter generalization bounds and establishing a guarantee against pseudo-equations. Experimentally, PG-SR outperforms state-of-the-art baselines across diverse domains, maintaining robustness to varying prior quality, noisy data, and data scarcity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Prior-Guided Symbolic Regression: Towards Scientific Consistency in Equation Discovery》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的符号回归（SR）方法容易陷入**“伪方程陷阱”（Pseudo-Equation Trap）**。即模型为了最小化经验风险（拟合误差），会生成在训练集上表现完美但在物理逻辑上荒谬的方程。
*   **研究背景**：在真实科学场景中，观测数据往往存在噪声且稀缺。传统的搜索算法（如遗传算法）、Transformer模型或基于大模型（LLM）的方法，由于缺乏显式的科学一致性约束，往往在分布外（OOD）预测时发生灾难性偏离。
*   **整体含义**：本文旨在通过引入显式的领域先验知识，将方程发现从单纯的“曲线拟合”转变为受科学原理约束的“原理建模”。

### 2. 方法论：PG-SR 框架
PG-SR 提出了一个包含三个阶段的流水线，并结合了理论保障：
*   **核心思想**：将领域先验编码为可执行的 Python 约束程序，并利用 LLM 的推理能力在约束下搜索方程空间。
*   **关键技术细节**：
    1.  **先验约束检查器（Prior Constraint Checker）**：通过 LLM 提取领域知识，结合人类专家验证，生成检查方程是否符合物理特性的程序（如：生物量为0时增长率必为0、温度响应的单峰性等）。
    2.  **先验退火约束评估（PACE）**：一种动态评分机制。在搜索初期允许一定程度的约束违反以保持多样性，随着进化进行，逐步收紧约束（退火），强制模型收敛到科学一致的区域。
    3.  **三阶段流程**：
        *   **预热（Warm-up）**：基于数据统计特性生成初始方程骨架。
        *   **演化（Evolution）**：利用 LLM 作为演化算子，结合 PACE 评分在多个并行“岛屿”中迭代优化。
        *   **精炼（Refinement）**：通过残差分析识别误差来源，利用反射机制（Reflection）针对性地修正方程结构。
*   **理论保障**：论文证明了引入先验约束能有效降低假设空间的 **Rademacher 复杂度**，从而获得更紧致的泛化界限，从数学上保证了对伪方程的抑制。

### 3. 实验设计
*   **数据集/场景**：涵盖生物、材料、化学和物理四大领域：
    1.  **大肠杆菌增长**（复杂环境依赖）。
    2.  **材料应力-应变**（来自铝合金 6061-T651 的**真实实验数据**）。
    3.  **化学反应动力学 (CRK)**。
    4.  **非线性振荡器 1 & 2**（含时变和非时变系统）。
*   **基准方法（13种对比方法）**：
    *   **搜索类**：PySR, GPLearn, DSR, uDSR, Operon, GP-GOMEA。
    *   **Transformer类**：TPSR, E2E, NeSymReS, PhyE2E。
    *   **LLM类**：LLM-SR, LaSR, DrSR。
*   **评估指标**：ID（分布内）和 OOD（分布外）的归一化均方误差（NMSE）。

### 4. 资源与算力
*   **硬件环境**：Intel Core i9-14900HX CPU，单张 **NVIDIA GeForce RTX 4090 GPU**。
*   **推理成本**：
    *   **时长**：单个基准测试完整运行约需 **10 小时**。
    *   **Token 消耗**：单次运行约消耗 1785 万个 Token（GPT-4o-mini）。
    *   **经济成本**：单次运行约 **4.70 美元**。
*   **模型后端**：主要使用 GPT-4o-mini，同时也测试了 Llama-3.3-70B 以验证鲁棒性。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同性质的数据集上进行了全面对比，每组实验均包含 ID 和 OOD 测试。
*   **消融实验**：针对预热阶段、PACE 机制和精炼阶段分别做了剔除实验，验证了各组件的必要性。
*   **鲁棒性测试**：
    *   **噪声鲁棒性**：在 1% 到 10% 的高斯噪声下测试。
    *   **数据稀缺性**：在数据量从 100% 缩减至 5% 的极端情况下测试。
    *   **先验质量敏感性**：测试了无先验、弱先验、错误先验和全先验对结果的影响。
*   **评价**：实验设计非常充分且客观，特别是引入了真实实验数据（应力-应变）和 OOD 泛化测试，有力地证明了方法在科学发现中的实用性。

### 6. 主要结论与发现
*   **性能领先**：PG-SR 在所有数据集上的 OOD 泛化性能均显著优于现有方法，NMSE 通常低出 1-2 个数量级。
*   **科学一致性**：通过 PACE 机制，模型能够发现不仅拟合数据，而且在物理结构上与真解高度一致的方程（如在振荡器 2 中几乎完美复现了真解）。
*   **鲁棒性强**：即使在数据极度稀缺（仅 5%）或存在高噪声的情况下，由于有先验知识的“锚定”作用，模型仍能保持稳定，不会像纯数据驱动方法那样崩溃。

### 7. 优点与亮点
*   **解决痛点**：直击符号回归中“拟合好但不科学”的长期痛点。
*   **PACE 机制**：巧妙地平衡了搜索初期的探索（Exploration）和后期的约束（Constraint）。
*   **理论与实践结合**：不仅有统计学习理论的复杂度证明，还通过 LLM 实现了复杂的非线性方程发现。
*   **真实数据验证**：在缺乏真解的真实材料实验数据上表现优异，证明了其工程应用潜力。

### 8. 不足与局限
*   **人工干预**：虽然使用了 LLM 辅助，但在构建“可执行先验程序”时仍需要一定程度的人类专家介入和验证，尚未实现完全自动化。
*   **计算成本**：依赖商业 LLM API，对于大规模并行搜索或预算有限的研究者来说，Token 成本和 API 延迟是潜在障碍。
*   **先验依赖**：如果提供的先验知识完全错误，虽然 PACE 能缓解崩溃，但仍会对最终方程的准确性产生负面影响。

（完）
