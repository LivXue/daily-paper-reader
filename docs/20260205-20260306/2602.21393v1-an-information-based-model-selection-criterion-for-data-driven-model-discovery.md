---
title: An information-based model selection criterion for data-driven model discovery
title_zh: 一种用于数据驱动模型发现的基于信息的模型选择准则
authors: "Michael C Chung, Alen Zacharia, Juan Guan"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21393v1"
tags: ["query:sr"]
score: 10.0
evidence: 利用稀疏回归从数据中提取可解释的符号模型
tldr: 针对数据驱动模型发现中难以平衡模型复杂性与准确性的问题，本文提出了一种新型信息准则 SLIC 及其配套的稀疏回归算法。该方法能自动生成候选模型并利用 SLIC 准则自适应地筛选出最优模型，有效避免了人工调参和过拟合。通过在多种非线性微分方程及流体力学、纳米技术实验数据上的验证，证明了 SLIC 在提取可解释物理模型和生成可测试预测方面显著优于传统准则。
selection_source: fresh_fetch
motivation: 现有的数据驱动模型发现算法在平衡拟合度与稀疏性时常需繁琐的人工调参，且容易出现过拟合现象。
method: 提出一种自动生成候选模型的稀疏回归算法，并引入样本长度缩放对数信息准则（SLIC）来识别最优模型。
result: 实验表明 SLIC 在处理非线性微分方程和真实实验数据时，其模型提取准确率显著超过了其他流行的方法选择准则。
conclusion: 该研究为从复杂数据中自动发现具有可解释性和预测能力的科学模型提供了一种稳健且高效的自动化工具。
---

## 摘要
数据驱动的模型发现 (DDMD) 算法是从数据中提取可解释符号模型的强大工具。然而，识别出最佳平衡拟合优度与稀疏性的模型通常是一个费力的过程，需要用户进行微调，且容易出现过拟合，结果可能会因模型初始化和特定训练过程的不同而产生显著差异。在本文中，我们提出了一种稀疏回归算法，该算法能够自动且自适应地生成候选模型，并使用一种新型的样本长度缩放对数信息准则 (SLIC) 从这些候选模型中识别出最佳模型。我们证明了在从几种非线性常微分方程和偏微分方程的数据中提取正确模型方面，SLIC 的表现显著优于其他流行的信息准则。随后，我们展示了 SLIC 从流体动力学和纳米技术的实验数据集中发现可解释模型的能力，这些模型能够产生新的可验证预测。

## Abstract
Data-driven model discovery (DDMD) algorithms are powerful tools for extracting interpretable symbolic models from data. However, identifying the model that best balances goodness-of-fit and sparsity is often a laborious process requiring user fine-tuning, is prone to overfitting, and results may significantly vary depending on model initialization and specific training procedure. Here, we present a sparse regression algorithm that automatically and adaptively generates candidate models, and uses a novel sample-length-scaling logarithmic information criterion (SLIC) to identify the best model from these candidates. We demonstrate that SLIC greatly outperforms other popular information criteria in extracting the correct model from the data of several nonlinear ordinary and partial differential equations. Then, we demonstrate SLIC's ability to discover interpretable models from experimental datasets in fluid dynamics and nanotechnology that generate new testable predictions.

---

## 论文详细总结（自动生成）

这是一份关于论文《An information-based model selection criterion for data-driven model discovery》（一种用于数据驱动模型发现的基于信息的模型选择准则）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
数据驱动模型发现（DDMD）旨在直接从观测数据中提取简洁、可解释的数学方程（如微分方程）。其核心挑战在于**模型选择**：如何在模型精度（拟合优度）与简洁性（稀疏性）之间取得平衡。
*   **研究动机**：现有的模型选择准则（如 AIC, BIC 等）在处理大规模数据时，拟合误差项的权重往往远超稀疏惩罚项，导致算法倾向于包含多余项（过拟合）。此外，现有的稀疏回归方法通常需要人工微调阈值参数，难以实现完全自动化。
*   **核心目标**：开发一种无需人工干预、能随数据量自适应缩放、且能稳健识别真实物理规律的新型信息准则。

### 2. 方法论：核心思想与关键技术
论文提出了一种名为 **SLIC** 的模型选择准则及其配套的自动剪枝算法。
*   **SLIC 准则公式**：$SLIC(\hat{\epsilon}, k) = n \log(\hat{\epsilon} k)$
    *   其中 $n$ 是样本量，$\hat{\epsilon}$ 是均方误差（MSE），$k$ 是模型复杂度（非零参数数量）。
*   **核心思想**：
    *   **对称性与缩放**：SLIC 要求准则在误差和复杂度之间保持对称，且惩罚项随样本量 $n$ 线性缩放。这确保了随着数据增加，模型不会因为误差项权重过大而陷入过拟合。
    *   **方差最小化**：论文证明了在稀疏回归框架下，最小化 SLIC 等价于最小化模型的方差，从而提升了模型的泛化能力。
*   **算法流程（Auto-pruning SLIC）**：
    1.  使用伪逆获取初始模型估计。
    2.  **自适应阈值生成**：直接利用当前系数的绝对值作为候选剪枝阈值。
    3.  **迭代优化**：遍历候选阈值，对模型进行剪枝并重新计算系数，利用 SLIC 分数识别最优候选模型。
    4.  **弱形式转换**：采用 Galerkin 投影（Weak form）处理噪声数据，将数值求导转化为积分运算，增强鲁棒性。

### 3. 实验设计与 Benchmark
论文通过模拟数据和真实实验数据进行了广泛验证：
*   **模拟数据集**：
    *   **常微分方程 (ODEs)**：Lorenz 系统、Rössler 吸引子、Lotka-Volterra 模型、Brusselator、Van der Pol 振子、非线性摆。
    *   **偏微分方程 (PDEs)**：Burger's 方程、KdV 方程、Kuramoto-Sivashinsky (KS) 方程、非线性薛定谔方程 (NLS)、Sine-Gordon 方程。
*   **真实实验场景**：
    *   **流体动力学**：矩形水箱中液体的质心晃动动力学。
    *   **纳米技术**：RNA-脂质体集群的聚合速率（Smoluchowski 理论）。
*   **对比方法 (Benchmarks)**：AIC, AICc, BIC, HQIC, KIC, 以及 Bridge Criterion (BC)。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   **软件环境**：实验主要基于 **Julia 语言**实现，使用了 `DifferentialEquations.jl` 进行仿真，`Optimization.jl` 进行参数优化，以及 `Trapz.jl` 进行数值积分。由于稀疏回归和信息准则计算主要涉及线性代数运算，其计算开销通常远低于深度学习模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每种 ODE 和 PDE，在不同的噪声水平（最高达 40%）下进行了测试。
    *   每组实验重复运行 **25 次**随机噪声实例化，以获取统计意义上的平均准确率和假阳性率（FPR）。
    *   进行了针对采样频率、轨迹长度和参数变化的鲁棒性分析。
*   **充分性评价**：实验设计非常充分且客观。它不仅覆盖了经典的基准动力系统，还通过跨领域的真实实验验证了模型的**外推预测能力**（例如用无驱动数据训练的模型成功预测了受迫振动行为），这证明了该方法捕捉的是本质物理规律而非简单的曲线拟合。

### 6. 主要结论与发现
*   **性能优越性**：在所有测试的 ODE 和 PDE 系统中，SLIC 在识别正确模型项方面的准确率显著高于 AIC 和 BIC，且假阳性率（误报多余项）极低。
*   **抗噪性与稳健性**：SLIC 在高噪声环境下表现稳健，且其性能不随数据量的增加而退化（解决了传统准则在大数据下的过拟合倾向）。
*   **科学发现能力**：
    *   在流体实验中，SLIC 自动识别出 Duffing 系统模型，并准确预测了未见过的滞后现象和不稳定区间。
    *   在纳米实验中，SLIC 确定了常数核（Constant Kernel）是主导机制，并据此提出了通过稀释来控制颗粒生长的有效策略。

### 7. 优点与亮点
*   **完全自动化**：通过自适应阈值生成，消除了 DDMD 中最耗时的人工调参环节。
*   **理论优雅**：SLIC 的推导基于对称性和方差最小化原则，具有坚实的统计学基础。
*   **极佳的解释性**：提取的模型是纯粹的符号方程，直接对应物理机制。
*   **样本效率高**：即使在数据量较少或下采样的情况下，依然能保持较高的识别精度。

### 8. 不足与局限
*   **库函数依赖**：该方法仍属于“字典回归”范畴，其成功高度依赖于预定义的非线性函数库（Library）是否包含真实的物理项。如果真实项不在库中，算法无法发现它。
*   **计算复杂度随维度增长**：对于极高维度的系统，候选基函数的组合爆炸可能会带来计算挑战。
*   **超参数讨论较少**：虽然 SLIC 本身无参数，但配套的 Galerkin 投影中仍涉及积分窗口大小等预处理参数，尽管文中提到了使用 SSIM 自动选择，但其普适性仍需更多验证。

（完）
