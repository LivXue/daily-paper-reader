---
title: Towards a data-scale independent regulariser for robust sparse identification of non-linear dynamics
title_zh: 迈向一种用于非线性动力学鲁棒稀疏识别的数据尺度无关正则化器
authors: "Jay Raut, Daniel N. Wilke, Stephan Schmidt"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05201v1"
tags: ["query:sr"]
score: 10.0
evidence: 非线性动力学和控制方程的鲁棒稀疏识别
tldr: 针对数据归一化导致SINDy等稀疏回归方法失效的问题，本文提出了一种对数据缩放具有鲁棒性的新算法STCV。该算法放弃了传统的基于幅值的阈值筛选，转而采用无量纲的统计指标“系数存在度（CP）”来评估模型项的显著性。实验证明，STCV在处理带噪声的归一化数据时，能比传统方法更准确地识别出简洁且符合物理规律的非线性动力学方程，显著提升了系统辨识的可靠性。
selection_source: fresh_fetch
motivation: 传统SINDy方法依赖幅值阈值，在数据归一化或存在噪声时容易产生冗余且错误的模型。
method: 提出STCV算法，利用无量纲统计指标“系数存在度”代替幅值进行顺序阈值筛选，实现对数据缩放的不变性。
result: 在多个动力学系统和物理实验中，STCV在归一化噪声数据集上的表现显著优于STLSQ和E-SINDy。
conclusion: STCV通过消除缩放效应的影响，增强了稀疏系统辨识在实际工程应用中的自动化程度、可解释性和可信度。
---

## 摘要
数据归一化是工程和科学应用中常见且通常必要的预处理步骤，但它会严重扭曲基于幅值的稀疏回归方法对控制方程的发现。这一问题在非线性动力学稀疏识别（SINDy）框架中尤为突出，其中稀疏性的核心假设受到数据缩放与测量噪声之间相互作用的破坏。导致发现的模型可能是稠密的、不可解释的，且在物理上是错误的。为了解决这一关键漏洞，我们引入了变异系数顺序阈值法（STCV），这是一种新型、计算高效的稀疏回归算法，对数据缩放具有内在的鲁棒性。STCV 用一种无量纲的统计指标——系数存在性（Coefficient Presence, CP）取代了传统的基于幅值的阈值处理，该指标评估了模型库中候选术语的统计有效性和一致性。这种从幅值到统计显著性的转变使得发现过程对任意数据缩放具有不变性。通过对典型动力系统和实际工程问题（包括物理质量-弹簧-阻尼器实验）的全面基准测试，我们证明了 STCV 在归一化且含噪声的数据集上始终显著优于标准的顺序阈值最小二乘法（STLSQ）和集成 SINDy（E-SINDy）。结果表明，即使在其他方法失效的情况下，基于 STCV 的方法也能成功识别出正确的、稀疏的物理定律。通过减轻归一化的扭曲效应，STCV 使稀疏系统识别成为现实应用中更可靠、更自动化的工具，从而增强了模型的可解释性和可信度。

## Abstract
Data normalisation, a common and often necessary preprocessing step in engineering and scientific applications, can severely distort the discovery of governing equations by magnitudebased sparse regression methods. This issue is particularly acute for the Sparse Identification of Nonlinear Dynamics (SINDy) framework, where the core assumption of sparsity is undermined by the interaction between data scaling and measurement noise. The resulting discovered models can be dense, uninterpretable, and physically incorrect. To address this critical vulnerability, we introduce the Sequential Thresholding of Coefficient of Variation (STCV), a novel, computationally efficient sparse regression algorithm that is inherently robust to data scaling. STCV replaces conventional magnitude-based thresholding with a dimensionless statistical metric, the Coefficient Presence (CP), which assesses the statistical validity and consistency of candidate terms in the model library. This shift from magnitude to statistical significance makes the discovery process invariant to arbitrary data scaling. Through comprehensive benchmarking on canonical dynamical systems and practical engineering problems, including a physical mass-spring-damper experiment, we demonstrate that STCV consistently and significantly outperforms standard Sequential Thresholding Least Squares (STLSQ) and Ensemble-SINDy (E-SINDy) on normalised, noisy datasets. The results show that STCV-based methods can successfully identify the correct, sparse physical laws even when other methods fail. By mitigating the distorting effects of normalisation, STCV makes sparse system identification a more reliable and automated tool for real-world applications, thereby enhancing model interpretability and trustworthiness.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：STCV 算法分析

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**数据归一化（Normalisation）对非线性动力学稀疏识别（SINDy）框架的破坏性影响**。
*   **背景**：在工程实践中，不同状态变量（如位移与速度）的量级差异巨大，通常需要归一化到 $[-1, 1]$ 以保证数值稳定性。
*   **动机**：传统的 SINDy 优化器（如 STLSQ）依赖系数的**绝对幅值**进行阈值筛选。然而，归一化会扭曲系数的景观，结合测量噪声后，会导致错误的（过拟合）项幅值反而超过真实的物理项。这使得发现的模型变得稠密、不可解释且物理错误。
*   **意义**：研究旨在开发一种对数据缩放不敏感、在噪声环境下更鲁棒的稀疏回归算法，以实现自动化的科学发现。

#### 2. 论文提出的方法论
论文提出了 **STCV（Sequential Thresholding of Coefficient of Variation，变异系数顺序阈值法）**。
*   **核心思想**：将筛选标准从“系数的大小”转变为“系数的统计一致性”。物理真实的项在不同噪声实现下应表现出稳定的估计值，而噪声诱导的错误项则会剧烈波动。
*   **关键指标：系数存在度（Coefficient Presence, CP）**：
    *   定义为变异系数（CV）倒数的缩放版本：$CP_{ij} = \frac{\sqrt{m} \cdot \mu_{\xi_{ij}}}{\sigma_{\xi_{ij}}}$。
    *   其中 $\mu$ 和 $\sigma$ 分别是系数的均值和标准差，$m$ 是样本数。CP 值越高，代表该项存在的统计显著性越强。
*   **技术细节**：
    *   **贝叶斯线性回归（BLR）**：为了避免昂贵的蒙特卡洛采样，STCV 使用带有弱先验的 BLR。通过解析解直接获得系数后验分布的均值和协方差矩阵，计算效率极高。
    *   **迭代流程**：采用类似模拟退火的策略，初始使用高岭回归惩罚和低 CP 阈值以保证稳定，随后逐步减小惩罚并提高 CP 阈值，迭代剔除统计不显著的候选项。
    *   **级联模式（STCV-STLSQ）**：STCV 先进行初步稀疏化以改善条件数，再由 STLSQ 完成最终精细识别。

#### 3. 实验设计
研究通过多层次的 Benchmark 验证了 STCV 的有效性：
*   **典型动力学系统**：Lorenz 系统、Rössler 系统、Van der Pol 振子、Duffing 振子。
*   **工程模拟场景**：
    *   **受损轴承模拟**：位移与速度量级相差 30,000 倍，必须归一化。
    *   **半车模型（Half-Car Models）**：包括线性和非线性版本，涉及 8 个状态变量的高维复杂系统。
*   **物理实验验证**：搭建了一个真实的质量-弹簧-阻尼物理实验台，使用 IMU 采集加速度数据，对比不同算法对真实物理定律的恢复能力。
*   **对比方法**：标准顺序阈值最小二乘法（STLSQ）和集成 SINDy（E-SINDy，带数据袋装/Bagging 选项）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU/CPU）或具体的训练时长。
*   **算法特性推断**：由于 STCV 基于贝叶斯线性回归的解析解（矩阵运算），其计算开销远低于基于 MCMC 采样的贝叶斯方法或深度学习方法，通常在普通个人电脑上即可快速运行。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   数值模拟部分：针对每个系统，在不同噪声水平下进行了 **1000 次**独立的随机噪声实现实验。
    *   超参数搜索：对所有对比方法（STLSQ, E-SINDy, STCV）均进行了广泛的超参数网格搜索，以确保对比的公平性（即展示每种方法在最佳参数下的表现）。
*   **充分性评价**：实验设计非常充分且客观。它不仅覆盖了低维到高维的数值模型，还包含了必须归一化的极端工程案例以及真实的物理实验数据，验证了算法从理论到实践的闭环。

#### 6. 论文的主要结论与发现
*   **归一化陷阱**：证实了在含噪数据归一化后，STLSQ 和 E-SINDy 的识别成功率会迅速降至 0%，因为它们无法区分被缩放后的真实项与噪声项。
*   **STCV 的优越性**：STCV 在归一化数据上的表现几乎与未缩放数据一致，且在噪声容忍度上显著优于 E-SINDy。
*   **物理一致性**：在真实物理实验中，只有 STCV 成功识别出了简洁且符合物理常识（线性/非线性项明确）的模型，而其他方法产生了大量物理上不可信的冗余项。
*   **无偏性**：通过偏置测试证明，STCV 对线性项和非线性项没有明显的选择偏好，能够准确捕捉动力学特征的“切换”。

#### 7. 优点
*   **尺度不变性**：彻底解决了 SINDy 在实际应用中必须面对的数据预处理（归一化）导致的失效问题。
*   **计算高效**：利用 BLR 解析解代替采样，兼顾了统计稳健性和计算速度。
*   **鲁棒性强**：在极高噪声水平下仍能保持较高的模型识别成功率。
*   **易于集成**：可以作为现有 SINDy 框架的插件或预处理步骤。

#### 8. 不足与局限
*   **纯净数据失效风险**：在完全无噪声的数据上，由于数值积分误差可能具有高度统计一致性，STCV 可能会误选过拟合项（论文建议通过注入微量噪声解决）。
*   **超参数依赖**：虽然 CP 指标是无量纲的，但 CP 阈值（$\lambda_{CP}$）和岭回归惩罚项仍需根据具体问题进行一定程度的调节或网格搜索。
*   **模型形式限制**：目前仍局限于预定义的候选函数库，对于库外项的识别能力有限（这是所有 SINDy 类方法的共同局限）。

（完）
