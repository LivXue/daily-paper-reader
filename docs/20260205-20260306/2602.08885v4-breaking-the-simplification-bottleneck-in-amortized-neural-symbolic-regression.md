---
title: Breaking the Simplification Bottleneck in Amortized Neural Symbolic Regression
title_zh: 突破摊销神经符号回归中的简化瓶颈
authors: "Paul Saegert, Ullrich Köthe"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08885v4"
tags: ["query:sr"]
score: 10.0
evidence: 通过快速简化引擎改进摊销神经符号回归
tldr: 符号回归（SR）旨在发现可解释的数学表达式，但摊销式神经符号回归在处理复杂科学问题时受限于表达式简化的效率瓶颈。本文提出 SimpliPy，一个比 SymPy 快 100 倍的规则化简化引擎，并构建了 Flash-ANSR 框架。该方法显著提升了训练和推理速度，支持更大规模的数据集训练和有效的测试集去重，在保持高精度的同时生成更简洁的表达式，性能可媲美最先进的遗传算法。
selection_source: fresh_fetch
motivation: 现有的摊销式符号回归方法受限于通用计算机代数系统极慢的简化速度，难以扩展到复杂的科学场景。
method: 提出了一种名为 SimpliPy 的高效规则化简化引擎，并基于此构建了 Flash-ANSR 框架，以实现大规模训练和高效的表达式规范化。
result: SimpliPy 实现了 100 倍的加速，使 Flash-ANSR 在基准测试中优于同类神经模型，并达到与 PySR 相当的精度且表达式更简洁。
conclusion: 通过突破表达式简化的计算瓶颈，摊销式神经符号回归能够在大规模数据集上实现更高效、更准确且更简洁的公式发现。
---

## 摘要
符号回归 (SR) 旨在发现能够准确描述观测数据的可解释解析表达式。摊销 SR 有望比主流的遗传规划 SR 方法更高效，但目前在扩展到现实科学复杂度方面仍面临挑战。我们发现，一个关键障碍是缺乏将等价表达式快速归约为简洁规范形式的方法。摊销 SR 曾通过 SymPy 等通用计算机代数系统 (CAS) 来解决这一问题，但高昂的计算成本严重限制了训练和推理速度。我们提出了 SimpliPy，这是一种基于规则的简化引擎，在质量相当的情况下，其速度比 SymPy 快 100 倍。这使得摊销 SR 得到了实质性的改进，包括对更大训练集的扩展性、对每个表达式 token 预算的更高效利用，以及针对等价测试表达式的系统性训练集去污染。我们在 Flash-ANSR 框架中展示了这些优势，该框架在 FastSRB 基准测试中取得了比摊销基准模型（NeSymReS、E2E）更好的准确率。此外，它的性能与最先进的直接优化方法 (PySR) 相当，同时随着推理预算的增加，它能恢复出更简洁而非更复杂的表达式。

## Abstract
Symbolic regression (SR) aims to discover interpretable analytical expressions that accurately describe observed data. Amortized SR promises to be much more efficient than the predominant genetic programming SR methods, but currently struggles to scale to realistic scientific complexity. We find that a key obstacle is the lack of a fast reduction of equivalent expressions to a concise normalized form. Amortized SR has addressed this by general-purpose Computer Algebra Systems (CAS) like SymPy, but the high computational cost severely limits training and inference speed. We propose SimpliPy, a rule-based simplification engine achieving a 100-fold speed-up over SymPy at comparable quality. This enables substantial improvements in amortized SR, including scalability to much larger training sets, more efficient use of the per-expression token budget, and systematic training set decontamination with respect to equivalent test expressions. We demonstrate these advantages in our Flash-ANSR framework, which achieves much better accuracy than amortized baselines (NeSymReS, E2E) on the FastSRB benchmark. Moreover, it performs on par with state-of-the-art direct optimization (PySR) while recovering more concise instead of more complex expressions with increasing inference budget.

---

## 论文详细总结（自动生成）

以下是对论文《Breaking the Simplification Bottleneck in Amortized Neural Symbolic Regression》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：符号回归（SR）旨在从观测数据中发现简洁、可解释的数学公式。传统的遗传规划（GP）方法（如 PySR）虽然精确但计算成本高，且无法利用先验知识。摊销式（Amortized）神经符号回归通过预训练 Transformer 模型来预测公式，理论上推理极快。
*   **核心问题**：**简化瓶颈（Simplification Bottleneck）**。
    *   随机生成的数学表达式存在大量冗余（如 $x+x$ vs $2x$）。
    *   现有的计算机代数系统（如 SymPy）处理速度极慢，导致在线生成简化训练数据的效率极低，限制了模型向复杂科学问题的扩展。
    *   若不进行简化，模型会学习到冗余的映射，导致推理效率低下且难以优化。

### 2. 论文提出的方法论
论文提出了 **Flash-ANSR** 框架，其核心是名为 **SimpliPy** 的简化引擎。
*   **SimpliPy 简化引擎**：
    *   **核心思想**：将代数简化转化为高效的模式匹配和查表操作，而非昂贵的树遍历。
    *   **离线规则发现**：利用 Kruskal 算法变体，在包含 4 个变量、7 个符号的范围内预先寻找最小生成森林，提取最简规则。
    *   **在线模式匹配**：在训练/推理时，通过哈希索引和子树匹配快速应用规则，并结合结合律/交换律的合并与抵消。
*   **Flash-ANSR 架构**：
    *   **Encoder**：采用改进的 Set Transformer，引入 Masked RMSSetNorm 处理变长点集。
    *   **Decoder**：预归一化（Pre-norm）Transformer Decoder，使用 FlashAttention 和旋转位置编码（RoPE）。
    *   **训练流程**：全在线（On-the-fly）生成 5.12 亿个简化后的表达式-数据对，支持高维输入（最高 10 维）和广泛的算子集。
*   **数据去污染（Decontamination）**：实施严格的符号和数值双重检查，确保训练集中不包含任何与测试集等价的表达式。

### 3. 实验设计
*   **数据集/场景**：使用 **FastSRB** 基准测试，该测试集基于物理规律（SRSD），比传统的 Feynman 或 Nguyen 数据集更符合真实科学场景。
*   **对比方法**：
    *   **摊销式基准**：NeSymReS（静态数据集代表）、E2E（不简化、在线生成代表）。
    *   **直接优化基准**：PySR（目前最先进的遗传规划方法）。
*   **评估指标**：数值恢复率（NRR，基于 FVU < $10^{-7}$）、表达式长度比（Parsimony）。

### 4. 资源与算力
论文详细列出了不同规模模型的训练资源（见附录 B）：
*   **模型规模**：提供 3M、20M、120M 和 1B 四种参数规模。
*   **硬件与时长**：
    *   **3M**：RTX 2080Ti，耗时 112 小时。
    *   **20M**：RTX 4090，耗时 56 小时。
    *   **120M**：A100，耗时 340 小时。
    *   **1B**：H200，耗时 657 小时。
*   **推理环境**：在 AMD Ryzen 9 9950X 和 RTX 4090 的工作站上进行时间归一化评估。

### 5. 实验数量与充分性
*   **实验规模**：训练使用了 **5.12 亿**个样本，测试涵盖了 115 个复杂物理方程，每个方程进行多次采样评估。
*   **充分性**：实验设计非常全面，包括：
    *   **推理时间缩放实验**：分析了从毫秒级到 1000 秒级的性能演变。
    *   **数据稀疏性实验**：测试了从 1 个点到 2048 个点的恢复能力。
    *   **噪声鲁棒性实验**：引入不同级别的加性高斯噪声。
    *   **消融实验**：验证了训练先验（如常数范围）对泛化性的影响。
*   **客观性**：论文特别强调了“去污染”和“时间归一化”对比，避免了以往研究中常见的测试集泄露和算力不平等问题。

### 6. 论文的主要结论与发现
*   **性能突破**：Flash-ANSR 显著优于之前的摊销式模型（NeSymReS, E2E）。120M 和 1B 模型在较大推理预算下达到了与 PySR 相当的精度。
*   **简洁性反转（Parsimony Inversion）**：这是一个重要发现。PySR 随着搜索时间增加，公式往往变得更复杂以拟合残差；而 Flash-ANSR 随着采样数增加，反而能找到更简洁、更接近真值的“针尖”公式。
*   **SimpliPy 效率**：在保持与 SymPy 相当的简化质量前提下，实现了 **100 倍**的速度提升，彻底解决了训练瓶颈。
*   **双 descent 现象**：在数据稀疏性实验中观察到了类似深度学习的“复杂度峰值”现象，即在极少数据点时模型倾向于过拟合出极复杂的公式。

### 7. 优点
*   **工程创新**：SimpliPy 引擎为符号回归的大规模训练提供了实用的基础设施。
*   **严谨性**：在数据去污染和评估标准上树立了新标杆，解决了领域内长期存在的评估不规范问题。
*   **可扩展性**：证明了增加模型参数和训练数据量能显著提升符号回归的泛化能力。

### 8. 不足与局限
*   **噪声鲁棒性**：在面对高噪声数据（$\eta \ge 10^{-2}$）时，Flash-ANSR 的表现不如 PySR。这是因为模型将噪声误认为是需要解释的复杂信号（分布偏移）。
*   **算子限制**：虽然支持广泛，但仍受限于预定义的算子集，对于超出词表的新算子无法直接处理。
*   **常数优化依赖**：模型生成的骨架仍需依赖 Levenberg-Marquardt 等经典算法进行常数微调，尚未实现完全的端到端数值预测。

（完）
