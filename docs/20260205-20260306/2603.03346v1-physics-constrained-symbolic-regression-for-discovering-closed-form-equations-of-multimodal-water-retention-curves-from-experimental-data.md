---
title: Physics-constrained symbolic regression for discovering closed-form equations of multimodal water retention curves from experimental data
title_zh: 物理约束符号回归用于从实验数据中发现多峰持水曲线的闭式方程
authors: "Yejin Kim, Hyoung Suk Suh"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2603.03346v1"
tags: ["query:sr"]
score: 10.0
evidence: 物理约束符号回归用于发现闭式方程
tldr: 针对具有多峰孔隙分布的多孔材料水分特征曲线建模难题，本研究提出了一种物理约束符号回归框架。该方法利用遗传算法演化数学表达式，并将物理约束融入损失函数，从而从实验数据中自动发现具有物理一致性的闭合形式方程。该框架克服了传统叠加模型解释性差的缺点，为多尺度多孔介质的水力建模提供了新途径。
selection_source: fresh_fetch
motivation: 传统水力模型难以捕捉多峰孔隙分布材料的复杂多尺度特性，且现有叠加方法在数据稀疏时缺乏解释性和泛化性。
method: 采用基于遗传算法的符号回归技术，将数学表达式表示为二叉树，并在损失函数中嵌入物理约束以确保结果的稳健性。
result: 成功发现了能够有效表征具有不同孔隙结构材料水分截留特性的闭合形式数学方程。
conclusion: 该物理约束机器学习框架为自动发现复杂多孔介质的物理一致性数学模型提供了一种高效且可解释的工具。
---

## 摘要
对具有多峰孔径分布的多孔材料的非饱和行为进行建模面临重大挑战，因为标准水力模型通常无法捕捉其复杂的、多尺度特征。一种常见的解决方法是叠加单峰持水函数，每个函数针对特定的孔径范围；然而，这种方法需要为每个模态单独进行参数识别，这限制了可解释性和泛化能力，特别是在数据稀疏的情况下。在这项工作中，我们引入了一种根本不同的方法：一种专为元建模设计的物理约束机器学习框架，能够直接从实验数据中自动发现多峰持水曲线的闭式数学表达式。数学表达式被表示为二叉树，并通过遗传编程进行演化，同时将物理约束嵌入到损失函数中，以引导符号回归器寻找物理一致且数学稳健的解。我们的结果表明，所提出的框架可以发现能够有效表征具有不同孔隙结构的多孔材料持水特性的闭式方程。为了支持第三方验证、应用和扩展，我们将完整的实现代码在开源仓库中公开。

## Abstract
Modeling the unsaturated behavior of porous materials with multimodal pore size distributions presents significant challenges, as standard hydraulic models often fail to capture their complex, multi-scale characteristics. A common workaround involves superposing unimodal retention functions, each tailored to a specific pore size range; however, this approach requires separate parameter identification for each mode, which limits interpretability and generalizability, especially in data-sparse scenarios. In this work, we introduce a fundamentally different approach: a physics-constrained machine learning framework designed for meta-modeling, enabling the automatic discovery of closed-form mathematical expressions for multimodal water retention curves directly from experimental data. Mathematical expressions are represented as binary trees and evolved via genetic programming, while physical constraints are embedded into the loss function to guide the symbolic regressor toward solutions that are physically consistent and mathematically robust. Our results demonstrate that the proposed framework can discover closed-form equations that effectively represent the water retention characteristics of porous materials with varying pore structures. To support third-party validation, application, and extension, we make the full implementation publicly available in an open-source repository.

---

## 论文详细总结（自动生成）

以下是对论文《Physics-constrained symbolic regression for discovering closed-form equations of multimodal water retention curves from experimental data》的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从稀疏且含有噪声的实验数据中，自动发现既符合物理定律又具有高度可解释性的多峰持水曲线（Water Retention Curve, WRC）闭式数学方程。
*   **研究动机**：
    *   **传统模型局限**：经典的单峰模型（如 van Genuchten）无法捕捉具有复杂孔隙结构材料的多尺度特征。
    *   **现有方案缺陷**：叠加多个单峰函数的传统做法参数识别困难且泛化性差；深度神经网络虽然表达能力强，但属于“黑盒”模型，缺乏物理一致性和解释性。
    *   **符号回归挑战**：传统的符号回归（SR）容易过拟合噪声，且生成的方程可能违反物理定律（如出现非单调行为或超出物理边界）。

### 2. 方法论
*   **核心思想**：提出一种**物理约束符号回归（PCSR）**框架，将数学表达式表示为二叉树，利用遗传编程（Genetic Programming）进行演化，并通过在损失函数中嵌入物理惩罚项来引导搜索。
*   **关键技术细节**：
    *   **表达式表示**：使用二叉树结构，节点包含变量（吸力 $s$）、常数和算子（+、×、sin、exp、ln 等）。
    *   **多目标损失函数**：$L = L_{data} + L_{phys} + L_{mode}$。
        *   $L_{data}$：均方误差（MSE），确保拟合实验数据。
        *   $L_{phys}$：物理约束惩罚，包括**单调性**（吸力增加时饱和度必须下降）、**边界值**（最大饱和度为1，残余饱和度趋于常数）和**导数约束**（端点处斜率为0）。
        *   $L_{mode}$：形状引导约束，通过计算二阶导数的变号次数来强制方程符合预设的峰值数量（Nmode）。
    *   **数据预处理**：对吸力进行对数变换并归一化到 [0, 1] 空间，以提高训练稳定性。
    *   **算法流程**：随机生成初始种群 -> 评估损失（含物理惩罚） -> 通过选择、交叉、变异演化 -> 迭代直至发现最优闭式方程。

### 3. 实验设计
*   **数据集/场景**：
    *   **单峰曲线**：级配不良砂土（Poorly graded sand）、粉质粘壤土（Clay loam）。
    *   **双峰曲线**：含砾石粉砂、级配良好含砂砾石、水稻土、砂土-高岭土混合物。
    *   **高阶多峰（合成数据）**：人工生成的 3 峰和 4 峰 WRC 数据，用于测试框架的扩展性。
*   **Benchmark（基准）**：
    *   **半经验模型**：van Genuchten (1980) 和 Durner (1994) 模型。
    *   **对比方法**：Vanilla SR（无约束的原始符号回归）和 PCSR（无模态约束版本）。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。
*   **软件环境**：基于 Julia 语言的 `SymbolicRegression.jl` 库开发。
*   **超参数**：固定迭代次数为 100 次，种群规模为 50，最大表达式复杂度限制为 150 个节点，配置了 100 个配置点（Collocation points）用于评估物理损失。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从单峰到四峰的 8 组不同材料/场景的实验，并在附录中针对单调性、边界值和有界性约束分别做了消融实验。
*   **充分性评价**：实验设计较为全面，对比了传统经验模型和无约束机器学习方法。通过引入合成高阶多峰数据，验证了模型在极端复杂情况下的稳健性。实验过程考虑了噪声影响，对比分析客观且公平。

### 6. 主要结论与发现
*   **物理一致性**：PCSR 能够有效消除 Vanilla SR 产生的非物理波动（如局部回升或超出 [0, 1] 范围）。
*   **模态控制**：引入 $L_{mode}$ 约束后，模型能准确捕捉预期的多峰特征，有效防止了在数据稀疏区域的过拟合。
*   **可解释性**：发现的闭式方程虽然有时结构复杂，但相比神经网络，其数学形式透明，可直接集成到现有的水力模拟软件中。
*   **性能优越**：在处理非典型或复杂多孔介质时，PCSR 发现的方程比传统的 van Genuchten 或 Durner 模型拟合度更高。

### 7. 优点
*   **物理与数据双驱动**：成功将热力学原理（单调性、边界条件）转化为优化约束，解决了纯数据驱动模型不可靠的问题。
*   **自动建模**：无需预设复杂的数学架构，能够根据数据特征自动演化出最合适的函数形式。
*   **开源贡献**：提供了完整的开源代码和数据集，增强了研究的可重复性。

### 8. 不足与局限
*   **滞后效应缺失**：目前仅针对主干燥过程（脱水曲线）建模，未考虑水分截留中的迟滞效应（Hysteresis）。
*   **先验依赖**：模态数量（Nmode）需要作为先验知识预设，如果对材料孔隙结构完全未知，可能影响建模效果。
*   **方程复杂度**：符号回归生成的方程可能包含多层嵌套的三角函数或指数函数，虽然是闭式的，但物理含义的直观解释仍有难度。
*   **计算效率**：遗传编程在大规模搜索空间下的计算开销通常高于梯度下降法，论文未详细讨论其在大规模数据集上的扩展效率。

（完）
