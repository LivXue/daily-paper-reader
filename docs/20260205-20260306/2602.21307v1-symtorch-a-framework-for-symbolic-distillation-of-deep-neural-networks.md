---
title: "SymTorch: A Framework for Symbolic Distillation of Deep Neural Networks"
title_zh: SymTorch：深度神经网络符号蒸馏框架
authors: "Elizabeth S. Z. Tan, Adil Soubki, Miles Cranmer"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21307v1"
tags: ["query:sr"]
score: 10.0
evidence: 将神经网络符号化蒸馏为物理定律
tldr: SymTorch是一个旨在简化深度神经网络符号蒸馏过程的框架。它通过将神经网络组件包装并利用PySR将其转换为可解释的数学公式，解决了数据传输和模型集成等工程难题。该框架支持GNN、PINN和Transformer等多种架构，并证明了通过符号替代MLP层可提升大语言模型的推理效率，为科学发现和模型压缩提供了新工具。
selection_source: fresh_fetch
motivation: 符号蒸馏虽然能发现物理规律，但由于复杂的工程障碍，难以集成到现有的深度学习工作流中。
method: 开发了SymTorch库，通过自动化数据采集、GPU-CPU传输管理及PySR集成，实现神经网络组件向数学方程的自动转换。
result: "在GNN、PINN和Transformer等架构上成功应用，并在LLM推理中通过符号化MLP层实现了8.3%的吞吐量提升。"
conclusion: SymTorch有效降低了符号蒸馏的技术门槛，为提升深度学习模型的可解释性和推理性能提供了高效方案。
---

## 摘要
符号蒸馏利用可解释的、闭式数学表达式来替换神经网络或其组件。这种方法在直接从训练好的深度学习模型中发现物理定律和数学关系方面展现出巨大潜力，但由于将符号回归集成到深度学习工作流中存在工程壁垒，其应用仍然受限。我们推出了 SymTorch，这是一个通过封装神经网络组件、收集其输入输出行为并利用 PySR 将其近似为人类可读方程，从而实现自动化蒸馏的库。SymTorch 解决了阻碍其应用的工程挑战：GPU-CPU 数据传输、输入输出缓存、模型序列化，以及神经网络与符号化前向传播之间的无缝切换。我们在包括 GNN、PINN 和 Transformer 模型在内的多种架构上演示了 SymTorch。最后，我们提出了一个通过用符号代理替换 MLP 层来加速大语言模型（LLM）推理的概念验证，在性能适度下降的情况下实现了 8.3% 的吞吐量提升。

## Abstract
Symbolic distillation replaces neural networks, or components thereof, with interpretable, closed-form mathematical expressions. This approach has shown promise in discovering physical laws and mathematical relationships directly from trained deep learning models, yet adoption remains limited due to the engineering barrier of integrating symbolic regression into deep learning workflows. We introduce SymTorch, a library that automates this distillation by wrapping neural network components, collecting their input-output behavior, and approximating them with human-readable equations via PySR. SymTorch handles the engineering challenges that have hindered adoption: GPU-CPU data transfer, input-output caching, model serialization, and seamless switching between neural and symbolic forward passes. We demonstrate SymTorch across diverse architectures including GNNs, PINNs and transformer models. Finally, we present a proof-of-concept for accelerating LLM inference by replacing MLP layers with symbolic surrogates, achieving an 8.3\% throughput improvement with moderate performance degradation.

---

## 论文详细总结（自动生成）

以下是对论文《SymTorch: A Framework for Symbolic Distillation of Deep Neural Networks》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度学习模型（尤其是科学计算和语言模型）虽然性能强大，但其内部逻辑是“黑盒”，难以提取简洁、人类可读的数学规律。
*   **研究动机**：
    *   **科学发现需求**：在物理学等领域，研究者需要闭式方程（如 $F=ma$）来理解底层规律，而不仅仅是数值预测。
    *   **工程障碍**：现有的符号回归（SR）工具（如 PySR）与深度学习框架（如 PyTorch）集成困难，涉及复杂的 GPU-CPU 数据传输、激活值缓存、模型序列化以及神经网络与符号方程的混合推理切换。
    *   **推理加速潜力**：简单的数学方程比密集的矩阵运算执行更快，可能成为模型压缩和加速的新路径。

### 2. 方法论
*   **核心思想**：开发一个名为 **SymTorch** 的库，通过包装（Wrapping）神经网络组件，自动采集其输入输出（I/O）行为，并利用符号回归将其蒸馏为闭式数学表达式。
*   **关键技术细节**：
    *   **SymbolicModel 包装器**：继承自 `nn.Module`，可包裹任意 PyTorch 模块或函数。
    *   **自动化流水线**：利用 Forward Hooks 自动记录激活值，处理跨设备（GPU 到 CPU）的数据移动和缓存。
    *   **符号回归集成**：后端调用 PySR（基于多群体进化算法），在精度和复杂度之间寻找帕累托最优（Pareto Front）方程。
    *   **混合推理切换**：支持 `switch_to_symbolic` 方法，在推理时无缝将神经层替换为解析方程，并支持 `torch.compile` 优化。
    *   **SLIME 实现**：内置了“超局部可解释模型无关解释”（SLIME），通过在特定输入点周围采样并拟合符号代理模型来解释局部行为。

### 3. 实验设计
论文通过四个具有代表性的案例验证了框架的有效性：
1.  **LLM MLP 替换**：针对 Qwen2.5-1.5B 模型，使用 PCA 进行降维后，用符号方程替换部分 MLP 层，测试 WikiText-2 数据集上的困惑度（Perplexity）与吞吐量。
2.  **GNN 物理规律发现**：在粒子动力学数据集上训练图神经网络（GNN），通过蒸馏边函数（Edge Function）提取引力、弹力等物理定律。对比了标准、瓶颈、L1 正则、KL 正则及剪枝（Pruning）等多种 GNN 变体。
3.  **PINN 方程恢复**：使用物理信息神经网络（PINN）求解一维热传导方程，并将其蒸馏回原始偏微分方程（PDE）的解析解形式。
4.  **LLM 算术运算分析**：对 Llama-3.2-1B 进行符号分析，研究其在加法、乘法、计数和温度转换任务中实际学习到的“近似函数”。

### 4. 资源与算力
*   **GPU 资源**：LLM 推理基准测试在 **Nvidia A100-SXM4-80GB** 上运行。
*   **CPU 资源（符号回归）**：符号回归搜索主要在 **Apple M4 Max SoC**（14 核 CPU）上完成。
*   **耗时示例**：
    *   拟合 LLM 的 3 个 MLP 块：约 7-8 小时。
    *   GNN 边模型蒸馏：约 10 分钟。
    *   PINN 蒸馏：少于 3 分钟。
    *   LLM 算术运算蒸馏：每个操作 5-15 分钟。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了从微观物理规律（GNN/PINN）到宏观语言模型（Transformer）的多种架构，实验设计较为全面。
*   **消融与对比**：
    *   在 GNN 实验中对比了 5 种不同的正则化和架构变体。
    *   在 LLM 实验中对比了 PCA 降维带来的损失与符号替换带来的额外损失。
    *   对比了多种开源小模型（如 TinyLlama, SmolLM）的吞吐量-性能帕累托曲线。
*   **客观性**：作者诚实地指出了符号替换导致的性能下降，并提供了 Baseline 和 Control（恒等变换）组进行对比，实验结果较为客观。

### 6. 主要结论与发现
*   **功能性**：SymTorch 成功降低了符号蒸馏的门槛，能够从训练好的模型中恢复出准确的物理定律（如 $1/r^2$ 关系）。
*   **LLM 加速**：通过替换 3/28 层的 MLP，实现了 **8.3% 的吞吐量提升**，且模型仍保持在性能-速度的帕累托前沿。
*   **可解释性**：揭示了 LLM 在处理简单算术时并非执行精确算法，而是学习到了带有系统误差的复杂近似函数。
*   **PINN 优势**：PINN 结合符号蒸馏比纯数据驱动的 SR 更容易在极少数据（10 个点）下恢复物理方程。

### 7. 优点
*   **工程化程度高**：解决了 PyTorch 与符号回归结合中最痛苦的数据管理和模型序列化问题。
*   **架构无关性**：不仅适用于物理模型，也展示了在 LLM 等通用架构上的潜力。
*   **混合建模**：允许模型部分是神经的，部分是符号的，为“神经符号 AI”提供了实用的工具。

### 8. 不足与局限
*   **可扩展性瓶颈**：符号回归（SR）随变量数量呈指数级增长，目前必须依赖 PCA 等降维手段，这导致了显著的性能损失（LLM 实验中大部分损失来自 PCA）。
*   **计算成本**：尽管单个组件蒸馏较快，但对于超大规模网络，全量蒸馏的搜索空间巨大且耗时。
*   **复杂度度量**：目前的复杂度定义（节点数）可能无法完全等同于人类的可理解性（例如 `sin(sin(x))` 与 `x+2` 复杂度相同但可读性不同）。
*   **泛化风险**：LLM 的符号代理是在特定分布（WikiText）上拟合的，其在分布外（OOD）任务上的表现尚未得到充分验证。

（完）
