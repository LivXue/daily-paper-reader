---
title: "GENSR: Symbolic Regression Based in Equation Generative Space"
title_zh: GENSR：基于方程生成空间的符号回归
authors: "Qian Li, Yuxiao Hu, Juncheng Liu, Yuntian Chen"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20557v1"
tags: ["query:sr"]
score: 10.0
evidence: 基于生成潜空间的符号回归框架
tldr: 符号回归（SR）旨在从数据中发现数学方程。针对传统方法在离散空间搜索导致反馈噪声大的问题，本文提出GenSR框架。该框架通过预训练条件变分自编码器（CVAE）构建具有符号连续性和局部数值平滑性的生成潜空间，将SR任务转化为“地图构建、粗略定位、精细搜索”的过程。GenSR在预测精度、表达式简洁度和计算效率上表现优异，且对噪声具有鲁棒性。
selection_source: fresh_fetch
motivation: 传统符号回归在离散空间搜索，方程结构微调与数值行为不一致，导致拟合误差反馈难以有效引导搜索。
method: 提出基于CVAE的生成潜空间框架，通过预训练将方程重参数化为平滑空间，并结合CMA-ES算法进行精细化搜索。
result: 实验表明GenSR在保持高预测准确率的同时，显著提升了表达式的简洁性和计算效率，并展现出强抗噪性。
conclusion: GenSR通过将符号回归重构为贝叶斯条件分布最大化问题，为在连续潜空间中高效搜索数学方程提供了理论保证和实践路径。
---

## 摘要
符号回归 (SR) 旨在揭示观测数据背后隐藏的方程。然而，大多数方法在离散方程空间中进行搜索，其中方程的结构修改很少与其数值行为一致，导致拟合误差反馈过于嘈杂，难以引导探索。为了应对这一挑战，我们提出了 GenSR，这是一个基于生成潜空间的符号回归框架，遵循“地图构建 -> 粗略定位 -> 精细搜索”的范式。具体而言，GenSR 首先预训练一个双分支条件变分自编码器 (CVAE)，将符号方程重新参数化为具有符号连续性和局部数值平滑性的生成潜空间。该空间可以被视为方程空间中结构良好的“地图”，为搜索提供方向信号。在推理阶段，CVAE 将输入数据粗略定位到潜空间中有潜力的区域。随后，改进的 CMA-ES 利用平滑的潜梯度对候选区域进行精细化处理。从贝叶斯角度来看，GenSR 将符号回归任务重新定义为最大化条件分布 $p(\mathrm{Equ.} \mid \mathrm{Num.})$，而 CVAE 的训练通过证据下界 (ELBO) 实现了这一目标。这一新视角为 GenSR 的有效性提供了理论保证。大量实验表明，GenSR 在保持对噪声鲁棒性的同时，协同优化了预测准确性、表达式简洁性和计算效率。

## Abstract
Symbolic Regression (SR) tries to reveal the hidden equations behind observed data. However, most methods search within a discrete equation space, where the structural modifications of equations rarely align with their numerical behavior, leaving fitting error feedback too noisy to guide exploration. To address this challenge, we propose GenSR, a generative latent space-based SR framework following the `map construction -> coarse localization -> fine search'' paradigm. Specifically, GenSR first pretrains a dual-branch Conditional Variational Autoencoder (CVAE) to reparameterize symbolic equations into a generative latent space with symbolic continuity and local numerical smoothness. This space can be regarded as a well-structured `map'' of the equation space, providing directional signals for search. At inference, the CVAE coarsely localizes the input data to promising regions in the latent space. Then, a modified CMA-ES refines the candidate region, leveraging smooth latent gradients. From a Bayesian perspective, GenSR reframes the SR task as maximizing the conditional distribution $p(\mathrm{Equ.} \mid \mathrm{Num.})$, with CVAE training achieving this objective through the Evidence Lower Bound (ELBO). This new perspective provides a theoretical guarantee for the effectiveness of GenSR. Extensive experiments show that GenSR jointly optimizes predictive accuracy, expression simplicity, and computational efficiency, while remaining robust under noise.

---

## 论文详细总结（自动生成）

### 论文总结：GenSR —— 基于方程生成空间的符号回归

#### 1. 核心问题与整体含义（研究动机和背景）
符号回归（SR）的核心挑战在于如何在庞大的数学表达式空间中找到最能解释观测数据的方程。传统方法（如遗传编程 GP、强化学习 RL）大多在**离散的符号空间**中进行启发式搜索。
*   **研究动机**：离散空间中方程的结构修改（如改变一个算子）与其数值行为的变化往往是不一致的（即“编辑距离”不等于“数值距离”）。这导致拟合误差的反馈信号充满了噪声，无法有效引导搜索方向，使得搜索过程类似于随机碰撞，效率低下且难以处理复杂方程。
*   **核心目标**：构建一个**连续且生成式的潜空间**（即方程的“地图”），将离散的符号搜索转化为连续空间的梯度或演化优化，从而实现高效、精准的方程发现。

#### 2. 方法论：核心思想与技术细节
GenSR 提出了“**地图构建 → 粗略定位 → 精细搜索**”的新范式：
*   **地图构建（双分支 CVAE）**：
    *   **后验分支（Posterior Branch）**：输入数值样本和符号方程，学习符号结构的连续性，确保潜空间向量能解码为合法的方程。
    *   **先验分支（Prior Branch）**：仅输入数值样本，学习数值特征与潜空间的映射，确保具有相似数值行为的方程在空间中靠近（局部数值平滑性）。
    *   **理论支撑**：从贝叶斯视角将 SR 建模为最大化条件概率 $p(\text{Equ.} | \text{Num.})$，通过优化证据下界（ELBO）来训练模型。
*   **粗略定位**：推理时，利用先验分支将观测数据映射到潜空间的一个高概率区域（高斯分布），完成初步定位。
*   **精细搜索（改进型 CMA-ES）**：
    *   在潜空间中利用**协方差矩阵自适应演化策略（CMA-ES）**不断收缩分布，寻找最优解。
    *   **关键改进**：为了应对高维潜空间（如 512 维）的计算压力，采用了**对角协方差假设**和**Top-k 维度更新策略**，仅优化最相关的维度，显著提升了搜索效率。

#### 3. 实验设计
*   **数据集**：采用了符号回归的标准基准测试集 **SRBench**，包含：
    *   **Feynman 数据集**：119 个来自物理学讲义的经典方程。
    *   **Strogatz 数据集**：14 个非线性动力系统挑战。
    *   **Black-box 数据集**：57 个真实世界的黑盒回归任务。
*   **对比方法**：涵盖了 18 种主流算法，包括：
    *   **启发式/遗传算法**：GPlearn, Operon, GP-GOMEA 等。
    *   **深度学习/强化学习**：DSR, E2ESR, SNIP, TPSR, RAG-SR 等。
    *   **物理启发式**：AI Feynman 2.0。
*   **评估指标**：拟合精度（$R^2$）、方程复杂度（Token 数量）、计算时间（秒）。

#### 4. 资源与算力
*   **硬件配置**：使用了 **2 张 NVIDIA H800 (80GB)** GPU。
*   **训练细节**：
    *   预训练数据集包含约 500 万个合成方程-样本对。
    *   训练时长：共 200 个 epoch，每个 epoch 约 1 小时（总计约 200 小时）。
    *   模型参数量：约 1.62 亿（162.1M）。

#### 5. 实验数量与充分性
*   **实验规模**：在三大基准数据集上进行了全面测试，并引入了不同水平的**目标噪声**（0.001 到 0.1）来测试鲁棒性。
*   **消融实验**：详细探讨了潜空间维度（64 到 768）、CMA-ES 的种群大小、步长、Top-k 更新频率以及适应度函数权重对结果的影响。
*   **额外验证**：通过 t-SNE 可视化展示了潜空间的结构化特性，并通过**方程分类任务**验证了潜空间特征的判别能力。
*   **评价**：实验设计非常充分，涵盖了精度、效率、简洁度和鲁棒性四个维度，对比基线广泛，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **帕累托最优**：GenSR 在精度、复杂度和效率的权衡中始终处于帕累托前沿（Rank-1），能够以更短的时间找到更简洁、更准确的方程。
*   **抗噪性强**：在存在噪声的情况下，GenSR 的性能下降比传统方法更缓慢，且方差更小，表现出极强的稳定性。
*   **空间特性**：实验证实了 GenSR 构建的潜空间具有**全局符号连续性**和**局部数值平滑性**，这使得连续优化算法（如 CMA-ES）在符号空间中生效成为可能。

#### 7. 优点：亮点与创新
*   **范式创新**：打破了离散搜索的局限，通过 CVAE 将符号回归转化为连续空间的分布收缩问题。
*   **理论完备**：提供了严谨的贝叶斯推导，将深度学习的 ELBO 优化与符号发现逻辑统一。
*   **搜索效率**：改进的 CMA-ES 巧妙解决了高维潜空间搜索的计算瓶颈。
*   **生成能力**：相比 SNIP 等判别式模型，GenSR 的生成式空间保证了插值点的可解码性，减少了“非法方程”的产生。

#### 8. 不足与局限
*   **预训练依赖**：模型的性能高度依赖于预训练合成数据的多样性，如果目标方程的结构完全超出了预训练分布，模型可能难以发现。
*   **复杂方程挑战**：为了防止过拟合，模型在训练时限制了方程长度，这可能导致其在发现极度复杂的长方程时能力受限。
*   **高维优化风险**：尽管采用了 Top-k 策略，但在极高维度的潜空间中，CMA-ES 仍可能陷入局部最优。

（完）
