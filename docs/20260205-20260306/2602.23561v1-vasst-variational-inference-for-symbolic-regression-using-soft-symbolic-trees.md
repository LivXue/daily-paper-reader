---
title: "VaSST: Variational Inference for Symbolic Regression using Soft Symbolic Trees"
title_zh: VaSST：基于软符号树的符号回归变分推理
authors: "Somjit Roy, Pritam Dey, Bani K. Mallick"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23561v1"
tags: ["query:sr"]
score: 10.0
evidence: 用于符号回归的变分推理
tldr: 符号回归旨在从数据中恢复显式物理定律，但现有方法在处理噪声和不确定性量化方面存在局限。本文提出 VaSST，一种基于变分推理的可扩展概率框架。通过引入“软符号树”将离散的表达式搜索转化为连续的梯度优化问题，实现了高效的结构搜索与原则性的不确定性量化。在 Feynman 数据库等实验中，VaSST 在结构恢复和预测精度上均优于现有最先进方法。
selection_source: fresh_fetch
motivation: 现有的符号回归方法多依赖启发式搜索且缺乏有效的不确定性量化，难以在庞大的离散组合空间中进行高效探索。
method: 提出一种基于变分推理的框架，通过软符号树将离散算子分配连续化，从而利用梯度下降优化符号表达式的后验分布。
result: 实验表明，VaSST 在 Feynman 符号回归基准测试中的结构恢复能力和预测准确度均达到了最先进水平。
conclusion: 该研究证明了通过连续松弛和变分推理可以有效解决符号回归中的组合优化难题，并提供了可靠的概率解释。
---

## 摘要
符号回归近年来在人工智能驱动的科学发现中备受关注，其目标是从数据中恢复显式的闭式表达式，以揭示潜在的物理定律。尽管取得了近期进展，现有方法仍主要由启发式搜索算法或数据密集型方法主导，这些方法通常假设低噪声环境且缺乏原则性的不确定性量化。完全概率化的表述较为稀缺，而现有的基于马尔可夫链蒙特卡罗（MCMC）的贝叶斯方法往往难以高效地探索符号表达式高度多模态的组合空间。我们提出了 VaSST，这是一个基于变分推理的可扩展符号回归概率框架。VaSST 采用了符号表达式树的连续松弛，称为“软符号树”（soft symbolic trees），其中离散的算子和特征分配被允许组件上的软分布所取代。这种松弛将天文数字级符号空间上的组合搜索转化为高效的基于梯度的优化问题，同时保留了连贯的概率解释。学习到的软表示导出了符号结构的后验分布，从而实现了原则性的不确定性量化。在模拟实验和 SRBench 中的费曼符号回归数据库（Feynman Symbolic Regression Database）上，与最先进的符号回归方法相比，VaSST 在结构恢复和预测精度方面均取得了优异的性能。

## Abstract
Symbolic regression has recently gained traction in AI-driven scientific discovery, aiming to recover explicit closed-form expressions from data that reveal underlying physical laws. Despite recent advances, existing methods remain dominated by heuristic search algorithms or data-intensive approaches that assume low-noise regimes and lack principled uncertainty quantification. Fully probabilistic formulations are scarce, and existing Markov chain Monte Carlo-based Bayesian methods often struggle to efficiently explore the highly multimodal combinatorial space of symbolic expressions. We introduce VaSST, a scalable probabilistic framework for symbolic regression based on variational inference. VaSST employs a continuous relaxation of symbolic expression trees, termed soft symbolic trees, where discrete operator and feature assignments are replaced by soft distributions over allowable components. This relaxation transforms the combinatorial search over an astronomically large symbolic space into an efficient gradient-based optimization problem while preserving a coherent probabilistic interpretation. The learned soft representations induce posterior distributions over symbolic structures, enabling principled uncertainty quantification. Across simulated experiments and Feynman Symbolic Regression Database within SRBench, VaSST achieves superior performance in both structural recovery and predictive accuracy compared to state-of-the-art symbolic regression methods.

---

## 论文详细总结（自动生成）

以下是对论文《VaSST: Variational Inference for Symbolic Regression using Soft Symbolic Trees》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在庞大且离散的数学表达式组合空间中，高效地搜索出既能准确拟合数据又具有物理可解释性的显式数学公式（符号回归，SR），并同时实现原则性的不确定性量化。
*   **研究背景**：
    *   传统的遗传算法（如 GP）计算复杂度高且易产生过于复杂的公式。
    *   深度学习方法通常需要海量数据且在噪声环境下表现脆弱。
    *   现有的贝叶斯符号回归方法（如 BMS、BSR）依赖马尔可夫链蒙特卡罗（MCMC）采样，在处理多模态组合空间时收敛缓慢，难以扩展到大规模数据集。

### 2. 方法论：VaSST 框架
*   **核心思想**：将离散的符号表达式树搜索问题转化为连续的变分推理（Variational Inference）优化问题。通过引入“软符号树”（Soft Symbolic Trees），利用梯度下降算法高效探索符号空间。
*   **关键技术细节**：
    *   **全二阶树骨架**：预设一个最大深度为 $D$ 的满二叉树结构，每个节点包含三个潜在变量：扩展指示符（决定是否为叶节点）、算子分配（从算子库中选择）和特征分配（从输入变量中选择）。
    *   **连续松弛（Continuous Relaxation）**：使用 **Binary Concrete** 分布松弛离散的结构决策，使用 **Gumbel-Softmax** 松弛算子和特征的选择。这使得原本不可导的离散树结构变得可微。
    *   **变分目标函数**：通过最大化证据下界（ELBO）来优化变分参数。ELBO 包含期望对数似然（衡量拟合度）和 KL 散度（衡量与先验的偏差）。
    *   **深度依赖先验**：引入随深度增加而降低的节点分裂概率，体现“奥卡姆剃刀”原则，惩罚过于复杂的表达式。
    *   **黑盒优化与退火**：使用 AdamW 优化器，并配合温度退火策略，使“软”表示在训练后期逐渐收敛向“硬”的离散结构。
    *   **硬树采样与排序**：优化完成后，从变分分布中采样多个候选“硬”树，根据样本内 RMSE 进行排序，从而提供不确定性量化。

### 3. 实验设计
*   **数据集与场景**：
    *   **模拟实验**：包含多项式函数和三角函数，设置了无噪声、低噪声（$\sigma^2=0.1^2$）和高噪声（$\sigma^2=0.2^2$）三种环境。
    *   **费曼符号回归数据库（FSReD）**：选取了库仑定律（CL）、引力势能（CPE）、洛伦兹力（FCE）和傅里叶热传导定律（FTC）四个典型物理公式。
*   **对比方法（Benchmarks）**：
    *   机器学习类：**QLattice**。
    *   遗传算法类：**gplearn**、**DEAP**。
    *   贝叶斯类：**BMS**（Bayesian Machine Scientist）、**BSR**（Bayesian Symbolic Regression）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率体现**：论文通过实验图表展示了 VaSST 在不同样本量（$n=2000$ 到 $5000$）下的运行时间，证明其计算效率显著优于基于 MCMC 的贝叶斯方法（BMS 和 BSR）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每个模拟场景进行了 10 次重复实验以评估稳定性。
    *   涵盖了从简单到复杂的多种数学结构。
    *   在费曼数据库上使用了 $n=2000$ 的子样本进行测试。
*   **充分性评价**：实验设计较为充分，不仅对比了预测精度（RMSE），还重点评估了“结构恢复能力”（即是否找回了原始公式）。通过引入不同噪声水平，客观地展示了该方法在现实科学数据环境下的鲁棒性。

### 6. 主要结论与发现
*   **结构恢复能力强**：VaSST 在所有测试的费曼方程中均能准确找回原始物理公式，而 QLattice 和 BSR 等方法常产生过于复杂的错误公式。
*   **预测精度高**：在噪声环境下，VaSST 的测试集 RMSE 始终处于领先地位，表现出极强的抗噪性。
*   **可扩展性优越**：相比于传统的贝叶斯采样方法，基于梯度的变分推理显著缩短了计算时间，更适合处理大规模科学数据。
*   **不确定性量化有效**：通过采样机制，VaSST 能提供多个候选公式及其排名，帮助科学家评估模型的可信度。

### 7. 优点（亮点）
*   **理论优雅**：成功将变分推理引入符号回归，解决了离散空间难以求导的痛点。
*   **平衡性好**：通过深度依赖先验，在模型复杂度和拟合精度之间找到了良好的平衡（Parsimony）。
*   **实用性强**：不仅给出一个结果，还给出一个概率分布，符合科学发现对“不确定性”的需求。

### 8. 不足与局限
*   **深度限制**：由于采用了固定深度的满二叉树骨架，如果目标公式极其复杂（深度超过预设值 $D$），模型可能无法捕捉。
*   **局部最优风险**：变分推理本质上是梯度下降，可能陷入局部最优解，对初始化有一定依赖。
*   **算子库依赖**：仍需预设算子集合（如 sin, exp 等），若目标公式包含未定义的特殊函数，则无法恢复。
*   **计算开销**：虽然比 MCMC 快，但相比于简单的线性回归，其软树评估过程（递归计算）在树深度较大时仍有一定计算负担。

（完）
