---
title: "KANDy: Kolmogorov-Arnold Networks and Dynamical System Discovery"
title_zh: KANDy：柯尔莫哥洛夫-阿诺德网络与动力系统发现
authors: "Kevin Slote, Jeremie Fish, Erik Bollt"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20413v1"
tags: ["query:sr"]
score: 9.0
evidence: 发现混沌和复杂动力系统的控制方程
tldr: 本研究提出KANDy，一种基于Kolmogorov-Arnold网络（KAN）的零深度、宽架构神经模型，旨在发现混沌及复杂动力系统的控制方程。KANDy通过将稀疏回归替换为KAN，克服了传统方法在稀疏性约束下的局限，并解决了KAN在动力系统应用中的不足。该模型在离散、连续系统及混沌偏微分方程中表现出色，能有效恢复拓扑结构，为非线性动力系统的数据驱动建模提供了一种可解释且高效的新方案。
selection_source: fresh_fetch
motivation: 旨在解决传统稀疏回归在复杂约束下的局限性，以及KAN在动力系统方程发现中的应用难题。
method: 引入KANDy架构，利用零深度的宽KAN网络替代稀疏回归来显式学习系统的控制方程。
result: 在Hopf纤维化、离散/连续动力系统及混沌PDE上均成功实现了方程发现与拓扑结构恢复。
conclusion: KANDy为非线性动力系统的建模提供了一个兼具解释性和有效性的替代方案。
---

## 摘要
我们引入了用于动力学的柯尔莫哥洛夫-阿诺德网络（KANDy），这是一种零深度、宽神经架构，能够发现混沌和复杂动力系统中的控制方程。在柯尔莫哥洛夫-阿诺德网络（KAN）的基础上，KANDy 通过用 KAN 替代稀疏回归，显式地学习控制方程。KAN 与稀疏回归的结合解决了 KAN 应用于动力系统时在方程发现方面的局限性，并克服了稀疏回归受限于稀疏性约束的情况。此外，我们展示了该模型在应用于 Hopf 纤维化时能够恢复拓扑结构，从而提高了与吸引子特性的相干性。我们将该模型应用于离散和连续动力系统，以及混沌偏微分方程（PDE）。这些结果使 KANDy 成为非线性动力系统数据驱动建模中一种具有可解释性且有效的替代方案。

## Abstract
We introduce the Kolmogorov-Arnold Network for Dynamics (KANDy) as a zero-depth, wide neural architecture capable of discovering governing equations in chaotic and complex dynamical systems. Building on the foundation of Kolmogorov-Arnold Networks (KANs), KANDy explicitly learns governing equations by replacing sparse regression with a KAN. The synthesis of KANs and sparse regression addresses the limitations of equation discovery for KANs applied to dynamical systems and overcomes cases where sparse regression is hindered by sparsity constraints. Additionally, we show that our model, applied to the Hopf Fibration, recovers topological structure, thereby improving coherence with attractor properties. We apply our model to discrete and continuous dynamical systems, as well as to chaotic partial differential equations (PDEs). These results position KANDy as an interpretable and effective alternative for data-driven modeling of nonlinear dynamical systems.

---

## 论文详细总结（自动生成）

以下是对论文《KANDy: Kolmogorov-Arnold Networks and Dynamical System Discovery》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在动力系统建模中，发现控制方程（Governing Equations）面临两大挑战：一是传统**稀疏回归方法**（如 SINDy）高度依赖方程的稀疏性假设，在处理非稀疏系统（如生物系统中的 Holling 项或光学中的 Ikeda 映射）时失效；二是新兴的 **KAN（柯尔莫哥洛夫-阿诺德网络）** 虽然具有可解释性，但在处理多变量非线性项（如 Lorenz 系统中的 $xy$ 耦合项）时，由于其单变量样条函数的限制，难以直接还原复杂的动力学结构。
*   **核心问题**：如何结合 KAN 的非线性拟合能力与稀疏回归的可解释性，构建一个既能处理非稀疏动力学，又能保持长期预测稳定性和拓扑一致性的方程发现框架？

### 2. 论文提出的方法论：KANDy
*   **核心思想**：提出 **KANDy（Kolmogorov-Arnold Network for Dynamics）** 架构。它摒弃了深度网络结构，采用**零深度（Zero-depth）、宽架构**的设计，将动力系统的状态通过“提升”（Lifting）技术映射到高维特征空间，再利用 KAN 进行方程发现。
*   **关键技术细节**：
    *   **提升特征（Lifted Features）**：将原始状态变量 $x$ 扩展为包含物理启发项（如 $xy, x^2, u u_x$ 等）的特征库 $\Phi(x)$。
    *   **零深度 KAN**：在提升后的特征上直接应用一层宽 KAN。这种设计避免了深度网络对全局方程结构的遮蔽，使每个边缘（Edge）的激活函数直接对应方程中的一个项。
    *   **混合损失函数**：结合了**导数监督损失**（局部向量场准确性）和**轨迹回滚损失（Rollout Loss）**（通过 ODE 求解器进行多步预测，确保全局轨迹一致性）。
    *   **符号公式提取**：开发了一套基于激活值的符号拟合程序，通过复杂度正则化（Complexity Regularization）在预定义的函数库中筛选最佳匹配项，实现从网络权重到闭式方程的转化。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **拓扑结构恢复**：Hopf 纤维化（Hopf Fibration），用于验证模型对流形拓扑结构的保持能力。
    *   **离散动力系统**：Hénon 映射、Ikeda 映射（非稀疏挑战）。
    *   **连续动力系统**：Lorenz 系统（经典混沌基准）。
    *   **偏微分方程（PDE）**：Kuramoto-Sivashinsky (KS) 方程（时空混沌）、无粘性 Burgers 方程（冲击波/不连续性挑战）。
*   **Benchmark 与对比方法**：主要对比了标准深度 KAN、传统稀疏回归（SINDy 逻辑）以及基于 MLP 的动力学模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 Python 和 `pykan` 库，训练参数包括：Lorenz 系统训练 300 个 Epoch，学习率为 $10^{-3}$，使用四阶龙格-库塔（RK4）积分器。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从低维常微分方程（ODE）到高维偏微分方程（PDE），从离散映射到连续流形的 5-6 种典型动力系统。
*   **充分性评估**：
    *   **多样性**：实验设计较为全面，涵盖了混沌、冲击波、拓扑不变性等多个维度。
    *   **鲁棒性测试**：进行了分布外（Out-of-distribution）初始条件的预测实验，并利用李雅普诺夫时间（Lyapunov Time）量化了预测失效边界。
    *   **客观性**：通过 NRMSE（归一化均方根误差）和符号恢复准确率进行定量评估，实验设计逻辑严密。

### 6. 论文的主要结论与发现
*   **方程发现能力**：KANDy 能够准确识别 Lorenz 和 KS 方程的各项系数，误差通常在 1%-15% 之间，且在稀疏回归失效的非稀疏系统（如 Ikeda 映射）中依然有效。
*   **预测稳定性**：引入回滚损失后，模型在混沌系统中的有效预测时长可达 5 个李雅普诺夫时间，远超普通数据驱动模型。
*   **拓扑一致性**：在 Hopf 纤维化实验中，KANDy 成功恢复了纤维束结构，而标准 KAN 出现了轨道塌陷。
*   **深度并非必要**：研究发现，对于动力系统发现，增加 KAN 的深度反而可能降低方程的可解释性和拟合稳定性，宽而浅的架构更具优势。

### 7. 优点
*   **架构创新**：巧妙地将 KAN 作为稀疏回归的非线性泛化，解决了 KAN 难以处理多变量乘积项的固有缺陷。
*   **物理可解释性**：通过零深度设计，使神经网络的每一部分都具有明确的物理含义，实现了“白盒化”建模。
*   **鲁棒性**：在处理包含冲击波（Burgers 方程）的不连续数据时表现出极强的数值稳定性。

### 8. 不足与局限
*   **特征库依赖**：KANDy 仍需预定义“提升特征”（Lifted Features），如果候选特征库未包含真实的物理项，模型性能会受限。
*   **计算开销**：虽然是零深度，但“宽架构”和“回滚损失”涉及通过 ODE 求解器进行反向传播，在大规模 PDE 上的计算成本可能较高。
*   **噪声敏感性**：论文主要在干净或低噪声数据上验证，在极端噪声环境下的方程发现鲁棒性有待进一步探讨。
*   **超参数调优**：符号提取过程中的复杂度权重和 $R^2$ 阈值需要人工干预，尚未实现完全自动化的模型选择。

（完）
